# 第 6 章 · 深度的代价——逐层预训练与 AlexNet 革命

> "Deep learning is a conspiracy by Hinton to get credit for backpropagation." — Yann LeCun（玩笑，但也不全是玩笑）

---

## Geoffrey Hinton、Alex Krizhevsky 和 Ilya Sutskever 是谁？

2006 年的多伦多，冬天漫长而寒冷。在多伦多大学计算机科学系的一间办公室里，一个留着灰白卷发、说话轻声细语的英国人正在白板上画图。他画了一堆圆圈和箭头，看起来像某种神秘的电路图。如果你不认识他，大概会以为这是一个迷失在大学里的退休工程师。

但 **Geoffrey Hinton** 不是迷失的人——他是这个领域里最固执的航海者。

Hinton 1947 年出生在英国伦敦，出身学术世家——他的曾曾祖父是乔治·布尔（George Boole），布尔代数就是以他命名的。Hinton 在剑桥大学读实验心理学，对"大脑如何学习"着了迷。但剑桥的心理学太偏哲学了，于是他又去爱丁堡大学读了一个人工智能的博士。从那以后，他的整个职业生涯都在做同一件事：**让神经网络工作**。

说"同一件事"可能不够准确——准确地说，他花了四十年做一件在绝大多数人看来"已经被证明不行"的事。1986 年他和 Rumelhart、Williams 一起让反向传播广为人知（第 3 章的故事），但到了 1990 年代末，连他自己都不得不承认：训练深层神经网络实在太难了。反向传播在两层、三层的网络上还行，一旦层数超过五层，梯度就消失了，训练就崩了。

整个 2000 年代，机器学习是 SVM 的天下。Vapnik 的理论保证（第 4 章）让资助机构和企业放心地投资，而神经网络被当成一种"没有理论基础的炼金术"。Hinton 的研究经费时有时无，他的实验室规模很小，博士生也不多。但他有一样东西是 SVM 阵营没有的：**一个坚定的信念**——深层网络一定是对的，因为人类大脑就是一个深层网络。

2006 年，Hinton 终于找到了突破口。他提出了一种叫做**逐层无监督预训练**（layer-wise unsupervised pretraining）的训练策略：不试图一次性训练整个网络，而是**一层一层地训练**——先让第一层学会从原始数据中提取简单特征，再把第一层的输出当输入训练第二层，以此类推。最后再用反向传播对整个网络进行微调。具体实现上，他用**受限玻尔兹曼机**（Restricted Boltzmann Machine, RBM）逐层预训练一个**深度自编码器**（deep autoencoder），它第一次让训练 10 层以上的网络成为可能。

这篇论文发表在 2006 年的 *Science* 杂志上，标题是 *"Reducing the Dimensionality of Data with Neural Networks"*。它在学术界引起了不小的震动——原来神经网络还没死！

但真正改变世界的事情发生在六年后的一个夏天。

**Alex Krizhevsky** 1986 年出生在苏联乌克兰，童年时随家人移民加拿大。他在多伦多大学读本科时选了 Hinton 的课，立刻被这个说话像念诗的老教授迷住了。Krizhevsky 是那种"安静的天才"——话不多，但写代码极快，而且有一种直觉，总能找到最简单有效的实现方式。

**Ilya Sutskever** 1986 年出生在苏联下诺夫哥罗德（当时称高尔基），五岁时随家人移居以色列，少年时又移民加拿大。他和 Krizhevsky 一样是 Hinton 的学生，但性格截然相反——Sutskever 充满激情，说话快，思考更快，对深度学习有一种近乎宗教般的信仰。多年后他成为 OpenAI 的联合创始人和首席科学家，推动了 GPT 系列的诞生。但在 2012 年，他还只是多伦多的一个博士生，满脑子只有一个想法：**用 GPU 训练一个巨大的卷积网络，去赢得 ImageNet 比赛**。

2012 年秋天，Krizhevsky 写的代码在一台配有两块 GTX 580 显卡的电脑上跑了一周。结果出来的那天，整个领域都安静了。

## 上一章的遗产：一个没解决的问题

第 5 章的 LSTM 解决了循环网络中的梯度消失，让序列建模成为现实。但 LSTM 的成功反而凸显了另一个更大的问题：**图像识别**仍然在用一种令人尴尬的老方法。

什么老方法？**手工设计特征**。

在 2012 年之前的计算机视觉领域，主流的工作流程是这样的：先用一套精心设计的手工算法把图像转换成一组数字——比如 **SIFT**（Scale-Invariant Feature Transform，尺度不变特征变换）或 **HOG**（Histogram of Oriented Gradients，方向梯度直方图）——然后把这些数字喂给 SVM 做分类。这个过程叫做"特征工程"（feature engineering）。

问题是，设计好特征需要大量的领域知识，而且每种任务需要不同的特征。检测人脸用一套特征，检测汽车用另一套，识别猫又得再来一套。更糟的是，这些特征是"浅层的"——它们只能捕获边缘、角点、纹理等底层视觉元素，无法组合成"猫的脸""汽车的车轮"这种高层语义概念。

为什么不直接用神经网络自动学特征？因为训练不动。第 3 章的反向传播在浅层网络（2-3 层）上工作得不错，但网络一旦变深（超过 5-6 层），就陷入三个泥潭：

1. **梯度消失**：和 RNN 中一样，梯度通过层层相乘逐渐衰减，前面的层几乎学不到东西。
2. **初始化敏感**：参数的初始值对最终结果影响巨大，稍微调不好就训练失败。
3. **过拟合**：深层网络参数量大，而标注数据（尤其是带标签的图像）很贵，模型容易死记硬背训练集而不是学到真正的规律。

在 2010 年左右，大多数研究者已经放弃了"训练深层网络"这个方向。ImageNet——一个包含 1400 多万张标注图像的超大数据集——每年的比赛中，最好的结果都来自 SIFT + SVM 或类似的浅层方法。

Hinton 的逐层预训练似乎提供了一条出路，但它的效果有限——在图像上，预训练方法的提升并不足以超越传统方法。深网络需要的不只是一种新的训练技巧——它需要一整套全新的"基础设施"。

## 核心洞察：一句话说清楚

**深度学习的突破不来自单一发明，而来自三个要素的会合：GPU 提供算力，ReLU 解决梯度消失，Dropout 防止过拟合——当这三者同时到位，深层网络的威力才真正释放。**

想象你要建一栋摩天大楼。2006 年的逐层预训练像是发明了一种新的打地基的方法——可以建更高了，但楼层越高风越大，材料也越贵，建到十层就建不动了。2012 年的突破相当于同时解决了三个问题：发明了一种不怕风的结构（ReLU），找到了一种便宜的防火材料（Dropout），还引进了巨型起重机（GPU）。突然之间，二十层、五十层都不在话下了。

## 数学原理

### 直觉解释

这一章涉及好几个概念，让我一个一个来。

**自编码器与逐层预训练。** Hinton 2006 年的方法基于一个叫**自编码器**（autoencoder）的巧妙想法。自编码器的原理很简单：让一个神经网络尝试把自己的输入"复制"到输出。但中间有一个"瓶颈"——网络中间层比输入小得多。比如输入是 100 维，中间层只有 10 维。网络被迫用 10 个数字来"压缩"再"解压"原始数据——就像用一句话概括一段话，再用这句话还原出大意。

如果压缩-解压后还能大致还原原始输入，说明中间那 10 个数字抓住了数据的本质特征。这些特征就是网络**自动学到的表示**——不需要人工设计，网络自己发现的。

Hinton 的逐层预训练就是反复做这件事：先训练一个自编码器学到第一层特征，再用这些特征作为输入训练第二个自编码器学到第二层特征，层层递进。每一层在前一层的基础上学到更抽象的表示。等所有层都"预热"好了，再接上一个分类器，用反向传播对整个网络进行微调。

这个方法有效，但很慢，而且在图像上的效果不够惊艳。真正改变游戏规则的是另一种架构——**卷积神经网络**（Convolutional Neural Network, CNN）。

**卷积。** 卷积的核心思想是**权重共享**。在全连接网络中，如果输入是一张 $28 \times 28$ 的图像（784 个像素），隐藏层的每个神经元都要和所有 784 个像素相连——参数量爆炸。但图像有一个很好的性质：一只猫的眼睛出现在左上角还是右下角，长得都一样——我们需要的是同一个"模式检测器"在不同位置扫描。

卷积就是这种扫描操作：一个小小的"检测器"（叫卷积核或滤波器，filter/kernel）在整张图像上滑动，在每个位置计算一个值。不管图像多大，卷积核的参数数量是固定的。这就是权重共享——同一个卷积核在所有位置重复使用。

**ReLU。** 传统的激活函数 sigmoid 和 tanh 有一个问题：当输入很大或很小时，函数的斜率（导数）接近 0。反向传播时，每经过一个这样的激活函数，梯度就缩小一点——层数一多，梯度就消失了。**ReLU**（Rectified Linear Unit，修正线性单元）用一种极其简单的方式解决了这个问题：$\text{ReLU}(x) = \max(0, x)$。当输入大于 0 时，导数恒为 1——梯度可以无损通过。这就好比把高速公路上的收费站全拆了，车流不再堵塞。

**Dropout。** 深层网络参数多，容易过拟合——模型把训练集死记硬背下来，遇到新数据就不行了。Dropout 的想法异常大胆：在训练过程中，随机"关掉"一半的神经元——让它们输出 0。这样网络就不能依赖任何一个神经元，每个神经元都必须学到有用的特征。这就像一个公司里如果任何员工都可能随时请假，那每个岗位都必须有备选方案——整个系统反而更健壮了。

### 关键公式

**1. 卷积运算——滑动窗口的点积：**

$$y_{i,j} = \sum_{m=0}^{k-1} \sum_{n=0}^{k-1} w_{m,n} \cdot x_{i+m, j+n} + b$$

其中 $x$ 是输入图像，$w$ 是 $k \times k$ 的卷积核，$b$ 是偏置，$y_{i,j}$ 是输出特征图在位置 $(i,j)$ 的值。直觉：在每个位置，卷积核和图像的一小块区域做点积，得到一个数值——衡量"这块区域和卷积核代表的模式有多匹配"。

**2. ReLU 激活——梯度高速公路：**

$$\text{ReLU}(x) = \max(0, x), \quad \frac{d}{dx}\text{ReLU}(x) = \begin{cases} 1 & x > 0 \\ 0 & x \leq 0 \end{cases}$$

当 $x > 0$ 时导数为 1，不衰减。这就是 ReLU 解决梯度消失的数学本质——在正半轴上，梯度可以无损传递。虽然一半的神经元被"关掉"了（$x \leq 0$ 时输出 0），但剩下的那一半传递梯度时不会衰减。

**3. Dropout——训练时随机失活：**

$$\tilde{h}_j = \begin{cases} h_j / p & \text{以概率 } p \text{ 保留} \\ 0 & \text{以概率 } 1-p \text{ 丢弃} \end{cases}$$

训练时每个神经元以概率 $1-p$ 被随机设为 0（$p$ 通常为 0.5），存活的神经元输出除以 $p$ 以保持期望值不变。测试时不做 Dropout。直觉：每次只训练网络的一个随机"子网络"，最终效果等价于很多子网络的集成（ensemble）。

### 手算例子

**第一部分：卷积的参数效率**

考虑一张 $5 \times 5$ 的灰度图像（像素值如下），和一个 $3 \times 3$ 的卷积核：

输入图像 $X$（$5 \times 5$）：

$$X = \begin{pmatrix} 1 & 2 & 0 & 1 & 3 \\ 0 & 1 & 3 & 2 & 1 \\ 2 & 3 & 1 & 0 & 2 \\ 1 & 0 & 2 & 3 & 1 \\ 3 & 2 & 0 & 1 & 2 \end{pmatrix}$$

卷积核 $W$（$3 \times 3$）：

$$W = \begin{pmatrix} 1 & 0 & -1 \\ 1 & 0 & -1 \\ 1 & 0 & -1 \end{pmatrix}$$

这个卷积核是一个**垂直边缘检测器**——它在检测图像中的竖直边界。

输出 $Y$ 是 $3 \times 3$（因为 $(5-3)/1 + 1 = 3$）。我们手动计算左上角的第一个值：

$$y_{0,0} = 1 \times 1 + 2 \times 0 + 0 \times (-1) + 0 \times 1 + 1 \times 0 + 3 \times (-1) + 2 \times 1 + 3 \times 0 + 1 \times (-1)$$
$$= 1 + 0 + 0 + 0 + 0 - 3 + 2 + 0 - 1 = -1$$

接着算 $y_{0,1}$（卷积核右移一格）：

$$y_{0,1} = 2 \times 1 + 0 \times 0 + 1 \times (-1) + 1 \times 1 + 3 \times 0 + 2 \times (-1) + 3 \times 1 + 1 \times 0 + 0 \times (-1)$$
$$= 2 + 0 - 1 + 1 + 0 - 2 + 3 + 0 + 0 = 3$$

类似地算出所有 9 个值：

$$Y = \begin{pmatrix} -1 & 3 & -2 \\ -3 & -1 & 2 \\ 3 & 1 & -2 \end{pmatrix}$$

现在对比参数效率：

- **全连接**：如果隐藏层有 $3 \times 3 = 9$ 个神经元，每个和所有 $5 \times 5 = 25$ 个输入相连，参数量 = $9 \times 25 + 9 = 234$。
- **卷积**：卷积核只有 $3 \times 3 + 1 = 10$ 个参数（9 个权重 + 1 个偏置），但它扫描了整张 $5 \times 5$ 的图像，输出了 $3 \times 3 = 9$ 个值。

参数量从 234 降到 10——少了二十多倍！而且卷积核学到的模式（这里是垂直边缘）在图像任何位置都能检测到，这就是权重共享的力量。

**第二部分：ReLU 如何保护梯度**

假设一个 5 层网络，每层都有一个 ReLU 激活。假设每层有一半的神经元处于正区间（导数为 1），一半处于负区间（导数为 0）。

使用 sigmoid 激活时，每层导数最大约 0.25（sigmoid 在大多数位置的导数远小于最大值 0.25）。5 层之后梯度大约变为 $(0.25)^5 = 0.001$——只剩千分之一。

使用 ReLU 时，处于正区间的神经元导数为 1。5 层之后，梯度仍然是 $1^5 = 1$——完全不变。

这就是为什么 ReLU 让深层网络的训练成为可能：它拆掉了梯度传播路上的收费站。

## 这解决了一切吗？——新的局限

AlexNet 的胜利是决定性的。2012 年 ImageNet 竞赛上，AlexNet 的错误率是 15.3%，而第二名是 26.2%——差距超过 10 个百分点，在竞赛中这简直是碾压级的优势。从那以后，ImageNet 竞赛的前列就再也没有出现过传统方法了。

深度学习的浪潮席卷了整个 AI。但这场革命留下了一个巨大的空白：它主要解决了**视觉**问题。

自然语言处理（NLP）仍然在一旁观望。当 AlexNet 在图像领域大杀四方的时候，NLP 的主流方法还是"词袋模型"（bag of words）和手工设计的语法特征。原因很简单：深度学习成功的关键之一是"让网络自动学习数据的表示"，但对于文本来说，最基本的问题还没解决——**怎么把一个词变成一个数字（向量）？**

在当时，每个词被表示为一个 **one-hot 向量**：假设词汇表有 10 万个词，"猫"就是第 3847 个位置为 1、其余位置全为 0 的 10 万维向量。"狗"是第 5621 个位置为 1 的向量。问题是：这种表示下，"猫"和"狗"的距离（点积为 0）等于"猫"和"桌子"的距离（也是 0）。所有词都是等距的——没有任何语义信息。

没有好的词表示方法，深度学习就无法有效地处理文本——就像 AlexNet 无法在图像上发挥作用，如果没有像素来表示图像的话。

与此同时，深度学习本身也面临质疑。一些研究者指出：这些深度网络实际上是"黑箱"——你知道它们管用，但你不知道为什么管用。SVM 至少能告诉你决策边界的数学形式，而深度网络有上亿个参数，没人能完全理解它在做什么。这种"不可解释性"在学术圈引发了持久的争论。

不过，在争论还在进行的时候，一个叫 Tomas Mikolov 的捷克研究者已经在 Google Brain 的办公室里，悄悄找到了一种把词变成向量的优雅方法。

## 这一年的世界

2012 年是 AI 历史上最重要的转折点之一，但当时的公众对此一无所知。那一年公众最关心的科技话题是 Facebook 的 IPO——这家社交网络公司以 1040 亿美元的估值上市，创下了当时科技公司上市的纪录。人们讨论的是"社交网络改变了人际关系"，而不是"神经网络即将改变一切"。

在学术界，AlexNet 的结果像一颗炸弹。ImageNet 竞赛是一个公开的、公认的基准测试——所有人都能看到排名，没有人能质疑结果的真实性。Hinton 的团队不仅赢了，而且赢了很多。赛后，几乎所有的计算机视觉研究者都开始转向深度学习。有些实验室在一夜之间抛弃了积累了十年的传统方法，这种场面在整个科学史上都不多见。

产业界的反应同样剧烈。2013 年初，Google 以约 4400 万美元收购了 Hinton 创办的初创公司 DNNresearch——这家公司只有三个人（Hinton、Krizhevsky 和 Sutskever）和一些想法，没有任何产品、收入或用户。百度据说也提出了收购意向，但出价不够高。从此以后，大型科技公司开始在 AI 人才上疯狂投资——给教授开出几倍甚至十倍于大学薪水的条件，让整个学术圈陷入了"人才争夺战"。

同一时期，GPU 巨头 NVIDIA 的命运也在悄然改变。它的 CEO 黄仁勋（Jensen Huang）做了一个关键决策：把 CUDA（GPU 的通用计算平台）开放给深度学习社区。这个决策在最初几年几乎没有商业回报，但到了 2012 年，当 AlexNet 证明了 GPU 对深度学习不可或缺之后，NVIDIA 从一家游戏显卡公司逐渐变成了 AI 基础设施的垄断者——这个转变的价值，在十年后将达到数万亿美元。

而在这一切喧嚣之下，一个问题安静地等待着被回答：深度学习能征服语言吗？

→ 深度学习征服了视觉，但一个诡异的现象出现了：更深的网络反而更差——20 层不如 8 层，50 层不如 20 层。深度明明是优势，为什么变成了负担？一条看似不起眼的"捷径"，将彻底改变这一切。
