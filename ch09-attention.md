# 第 9 章 · 学会"看哪里重要"——注意力机制

> "You can't cram the meaning of a whole sentence into a single vector." — 一个让 Dzmitry Bahdanau 熬夜思考的问题

---

## Dzmitry Bahdanau 是谁？

2014 年的蒙特利尔，冬天漫长而寒冷。在蒙特利尔大学（Université de Montréal）的 MILA 实验室里，一个来自白俄罗斯的博士生正在对着屏幕发愁。他的名字叫 **Dzmitry Bahdanau**（发音大约是"兹米特里·巴赫达瑙"），是 Yoshua Bengio 实验室的学生。

Bahdanau 在白俄罗斯的明斯克上了本科，就读于白俄罗斯国立大学的应用数学与计算机科学专业。之后他前往德国，在不来梅的雅各布大学（Jacobs University Bremen）攻读计算机科学硕士学位——正是在这里，他接触到了深度学习和自然语言处理的前沿研究。这种跨文化的学术经历让他对"语言"有一种特殊的敏感——他不只关心怎么让机器跑得更快，更关心机器能不能真正理解语言的结构。在 MILA 期间，他也参与了语音识别相关的研究——又是一个和"序列数据"打交道的工作。

Bahdanau 的导师 Bengio 是深度学习"三巨头"之一（另外两位是 Hinton 和 LeCun），但 MILA 实验室在 2014 年的规模远没有今天这么大。整个实验室也就二三十个博士生和博士后，挤在蒙特利尔大学的一栋旧楼里。但正是这种"小而精"的环境，让 Bahdanau 有机会和实验室里的其他研究者深入交流——特别是那些正在做机器翻译的人。

与此同时，故事还有另一位关键人物——**Ilya Sutskever**。Sutskever 是 Hinton 的学生，深度学习领域最出色的研究者之一。2012 年，他和 Alex Krizhevsky 一起实现了 AlexNet（第 6 章的主角）。2013 年初，Hinton 的公司 DNNresearch 被 Google 收购（DNNresearch 于 2012 年成立，2013 年 3 月完成收购），Sutskever 随之加入 Google Brain。在 Google，他开始思考一个新问题：能不能用神经网络做端到端的机器翻译？

2014 年，Sutskever 和他的合作者 Oriol Vinyals、Quoc Le 发表了一篇开创性的论文——**Sequence to Sequence Learning with Neural Networks**（序列到序列学习），简称 **Seq2Seq**。这篇论文提出了一个优雅的架构：一个 RNN（循环神经网络）把源句子"读"完，把所有信息压缩进一个固定长度的向量；另一个 RNN 从这个向量出发，一个词一个词地"写出"目标语言的翻译。

这个想法很漂亮——但有一个致命的缺陷，而这个缺陷，恰好给了 Bahdanau 大显身手的机会。

## 上一章的遗产：一个没解决的问题

第 8 章的 Word2Vec 让每个词有了自己的向量。这下，NLP 的工具箱终于齐全了——词是向量，神经网络可以处理向量，所以神经网络终于可以处理语言了，对吧？

没那么简单。Word2Vec 解决了"词"的表示问题，但没有解决"句子"的处理问题。翻译一个句子，你需要先理解整个源句子的含义，再用另一种语言重新表达。这是一个**序列到序列**（sequence to sequence）的任务——输入是一个变长的词序列，输出也是变长的词序列。

Sutskever 的 Seq2Seq 模型试图用一种优雅的方式解决这个问题。架构分为两部分：

**编码器**（encoder）：一个 RNN 从左到右依次读入源句子的每一个词。读到最后一个词时，RNN 的隐藏状态（hidden state）理论上包含了整个源句子的信息。这个最终的隐藏状态被称为**上下文向量**（context vector），通常记作 $\mathbf{c}$。

**解码器**（decoder）：另一个 RNN 从上下文向量 $\mathbf{c}$ 出发，一个词一个词地生成目标翻译。

听起来很合理。但让我们动手试一个具体的例子。假设你要翻译英语到法语：

> 源句：I love the beautiful city of Paris in springtime

这句话有 9 个词。编码器 RNN 依次读了 9 个词之后，把所有信息塞进了一个——比如说——500 维的向量 $\mathbf{c}$ 中。然后解码器只能看着这 500 个数字，来生成法语翻译。

现在想象一个更长的句子——50 个词。同样是 500 维的 $\mathbf{c}$。再用 500 个数字来概括 50 个词的信息？100 个词呢？

问题已经很清楚了：**不管源句子有多长，所有信息都被暴力压缩进了一个固定维度的向量。** 这就像让你读完整本《战争与和平》，然后用一句话总结——你当然可以做到，但大量细节必然丢失。

Bahdanau 在实验中观察到了这个问题的具体症状。当源句子很短（5-10 个词）时，Seq2Seq 的翻译质量还不错。但当句子变长到 15、20 个词以上时，翻译质量**急剧下降**——不是缓慢变差，而是像从悬崖上掉下去一样。编码器的 RNN 在读到最后几个词时，早就把开头的信息"忘"得差不多了（还记得第 5 章的梯度消失问题吗？这里的表现是一样的），所以上下文向量 $\mathbf{c}$ 主要包含了句子末尾的信息，开头的信息已经面目全非。

## 核心洞察：一句话说清楚

**与其让解码器只看一个压缩后的"总结"，不如让它在每一步生成时都能回头去看源句子的每一个位置，自己决定现在最该关注哪里。**

想象你在做同声传译——把一段英文演讲翻译成中文。当你听到"The GDP grew by 3% last quarter"时，你不需要先在脑子里背诵整段演讲的每一个字，再开始翻译。你会自然地"回头看"刚才听到的部分——翻译"3%"的时候，你关注的是"GDP"和"grew"；翻译"last quarter"的时候，你关注的是时间信息。你的注意力在不同时刻**动态地**聚焦于源句子的不同部分。

这就是 Bahdanau 的核心洞察：不要把信息压缩成一个静态向量，而是让解码器拥有一种"动态回顾"的能力——在生成每一个目标词时，自主决定源句子中哪些位置最相关。

## 数学原理

### 直觉解释

注意力机制的工作方式可以分三步来理解：

**第一步：打分。** 当解码器准备生成第 $j$ 个目标词时，它当前的隐藏状态 $\mathbf{s}_j$ 代表了"我现在想说什么"。编码器那边，源句子的每一个位置都有一个隐藏状态 $\mathbf{h}_i$，代表了"源句子的第 $i$ 个词附近说了什么"。我们用一个"打分函数"来衡量 $\mathbf{s}_j$ 和每个 $\mathbf{h}_i$ 之间的相关程度——分数越高，说明源句子的第 $i$ 个位置对当前要生成的目标词越重要。

**第二步：归一化。** 这些分数被 softmax 函数转化为一个概率分布——所有权重都是正数，加起来等于 1。这就像一个"注意力预算"：你对源句子所有位置的关注程度加起来恰好是 100%，关键是这 100% 怎么分配。

**第三步：加权求和。** 用这些权重对编码器的所有隐藏状态做加权平均，得到一个**上下文向量** $\mathbf{c}_j$。这个向量不是固定的——它随着解码器的每一步动态变化。当你要翻译"3%"时，上下文向量会主要包含编码器中"GDP"和"grew"附近的信息；当你要翻译"上季度"时，上下文向量会偏重时间相关的部分。

### 关键公式

**1. 注意力权重——从"相关性"到"概率"：**

$$\alpha_{ij} = \frac{\exp(e_{ij})}{\sum_{k=1}^{T_x} \exp(e_{kj})}$$

其中 $e_{ij} = \text{score}(\mathbf{h}_i, \mathbf{s}_j)$ 是打分函数，$T_x$ 是源句子的长度。直觉：$\alpha_{ij}$ 就是"在生成第 $j$ 个目标词时，源句子第 $i$ 个位置应该分配多少注意力"。softmax 保证所有注意力权重加起来等于 1。至于打分函数，Bahdanau 原始论文中用了一个小型前馈网络来学习 $\text{score}(\mathbf{h}_i, \mathbf{s}_j) = \mathbf{v}^{\top} \tanh(\mathbf{W}_a \mathbf{h}_i + \mathbf{U}_a \mathbf{s}_j)$，但更简单的方式是直接用**点积**：$e_{ij} = \mathbf{h}_i^{\top} \mathbf{s}_j$。

**2. 上下文向量——动态"摘录"源句子信息：**

$$\mathbf{c}_j = \sum_{i=1}^{T_x} \alpha_{ij} \mathbf{h}_i$$

直觉：把编码器的所有隐藏状态按注意力权重做加权求和。如果源句子第 3 个词的注意力权重是 0.7，其他位置只有 0.05，那上下文向量几乎就是第 3 个隐藏状态的一个近似"副本"——模型在说"我现在只需要看这个位置"。这个 $\mathbf{c}_j$ 与解码器当前状态 $\mathbf{s}_j$ 拼在一起，用来预测下一个目标词。

**3. 注意力的"软性选择"本质：**

$$\mathbf{c}_j = \sum_{i=1}^{T_x} \alpha_{ij} \mathbf{h}_i \approx \mathbf{h}_{k^*} \quad \text{当} \ \alpha_{k^*j} \approx 1 \ \text{时}$$

直觉：如果注意力高度集中在一个位置（某个 $\alpha \approx 1$，其余 $\approx 0$），上下文向量就近似于"硬选择"——只看那一个词。但通常注意力会分散在几个相关位置上，形成一种"软选择"——同时参考几个地方的信息，按重要性加权。这正是"注意力"比"固定取一个位置"更灵活的原因。

### 手算例子

让我们手动走一遍注意力计算的全过程。

**设定：** 源句子有 3 个词（法语），编码器产生了 3 个隐藏状态。解码器正在生成第 1 个目标词（英语），当前解码器状态为 $\mathbf{s}_1$。

| 位置 | 源词 | 编码器隐藏状态 $\mathbf{h}_i$（3 维） |
|---|---|---|
| 1 | Je | $(1.0, 0.0, 0.5)$ |
| 2 | suis | $(0.0, 1.0, 0.0)$ |
| 3 | étudiant | $(0.5, 0.0, 1.0)$ |

解码器当前状态：$\mathbf{s}_1 = (0.8, 0.1, 0.6)$

（第一个要翻译的目标词是"I"，所以解码器状态应该和"Je"最相关——"Je"就是法语的"I"）

**Step 1：用点积计算注意力分数。**

$$e_{11} = \mathbf{h}_1^{\top} \mathbf{s}_1 = 1.0 \times 0.8 + 0.0 \times 0.1 + 0.5 \times 0.6 = 0.8 + 0 + 0.3 = 1.1$$

$$e_{21} = \mathbf{h}_2^{\top} \mathbf{s}_1 = 0.0 \times 0.8 + 1.0 \times 0.1 + 0.0 \times 0.6 = 0 + 0.1 + 0 = 0.1$$

$$e_{31} = \mathbf{h}_3^{\top} \mathbf{s}_1 = 0.5 \times 0.8 + 0.0 \times 0.1 + 1.0 \times 0.6 = 0.4 + 0 + 0.6 = 1.0$$

**Step 2：Softmax 归一化。**

分母：$\exp(1.1) + \exp(0.1) + \exp(1.0) = 3.004 + 1.105 + 2.718 = 6.828$

$$\alpha_{11} = \frac{3.004}{6.828} = 0.440$$

$$\alpha_{21} = \frac{1.105}{6.828} = 0.162$$

$$\alpha_{31} = \frac{2.718}{6.828} = 0.398$$

注意力分布：**"Je"获得了 44% 的注意力，"étudiant"获得 40%，"suis"只有 16%。**

**Step 3：加权求和得到上下文向量。**

$$\mathbf{c}_1 = 0.440 \times (1.0, 0.0, 0.5) + 0.162 \times (0.0, 1.0, 0.0) + 0.398 \times (0.5, 0.0, 1.0)$$

$$= (0.440, 0.000, 0.220) + (0.000, 0.162, 0.000) + (0.199, 0.000, 0.398)$$

$$= (0.639, 0.162, 0.618)$$

**结果意味着什么？** 上下文向量 $\mathbf{c}_1 = (0.639, 0.162, 0.618)$ 主要由"Je"和"étudiant"的隐藏状态混合而成——"suis"几乎被忽略了。这完全符合我们的期望：在翻译"I"的时候，模型最关注源句子中对应的主语"Je"，同时稍微看了一眼"étudiant"（因为整个句子在说"我是学生"），但几乎不关注"suis"（动词"is"在翻译第一个词时还没轮到）。这个动态选择的上下文向量被拼接上 $\mathbf{s}_1$，送进一个线性层加 softmax，预测出目标词"I"。

**当解码器走到下一步，要翻译"am"时：** 解码器状态 $\mathbf{s}_2$ 会变（因为它接收了上一步的预测结果），注意力权重会重新计算——这一次"suis"的权重会大幅上升，因为"suis"正是法语的"am"。每一步都在重新分配注意力。

## 这解决了一切吗？——新的局限

注意力机制是深度学习中少有的"一击即中"的突破。Bahdanau 的论文发表后，几乎所有做序列到序列任务的研究者都迅速采纳了注意力机制。翻译质量显著提升——尤其是长句子的翻译，性能"悬崖"被大幅拉平了。

但注意力机制虽然解决了"信息压缩"的问题，它本身仍然依附于 RNN 架构——编码器和解码器都是循环神经网络。这意味着它继承了 RNN 的固有缺陷：

**第一，训练慢。** RNN 必须按时间步顺序计算——编码器必须先处理第 1 个词，再处理第 2 个词，再处理第 3 个词……无法并行。在 GPU 擅长大规模并行计算的时代，这种"一步一步来"的计算方式是对硬件能力的巨大浪费。一个 50 个词的句子，编码器至少需要 50 步串行计算。

**第二，梯度仍然要穿越时间步。** 虽然注意力机制缓解了信息传递的问题（不再只依赖最后一个隐藏状态），但 RNN 内部的梯度仍然需要通过反向传播穿越所有时间步。虽然 LSTM 的门控机制（第 5 章）缓解了梯度消失，但并没有完全消除。对于特别长的序列，训练仍然不够稳定。

**第三，注意力本身也带来了计算开销。** 对于源序列长度 $T_x$ 和目标序列长度 $T_y$，注意力机制需要计算一个 $T_x \times T_y$ 的注意力矩阵。当序列变长（比如处理整篇文章而非单句），这个矩阵的规模会二次增长——1000 个词的源序列和 1000 个词的目标序列意味着 100 万个注意力分数需要计算。

这些局限指向了同一个方向：能不能彻底抛弃 RNN 的顺序依赖？如果注意力机制已经证明了"直接看任意位置"是可行的，那我们为什么还需要循环结构一步一步地传递信息？

有没有可能——只用注意力，不用循环？

## 这一年的世界

2014 到 2015 年，深度学习正在从一个"小圈子运动"变成一场席卷整个科技行业的海啸。但有趣的是，推动这场变革的最大动力不是学术界，而是**工业界**。

Google 在 2014 年做了一件震动业界的事：以约 5 亿美元收购了 DeepMind——一家当时只有几十人的英国初创公司，由 Demis Hassabis、Shane Legg 和 Mustafa Suleyman 共同创立。DeepMind 在收购前最出名的成就是让 AI 学会玩 Atari 游戏，但这只是热身。收购后不到两年，DeepMind 的 AlphaGo 将在围棋上击败世界冠军李世乭——那将是 2016 年 AI 最轰动的事件。

在机器翻译领域，Google、百度、微软之间的竞争尤为激烈。Google Translate 在 2014 年仍然基于传统的统计机器翻译（SMT）——一种依赖大量人工设计特征的方法。Sutskever 的 Seq2Seq 论文在 Google 内部引起了极大兴趣，因为它承诺了一种"端到端"的替代方案：不需要任何语言学知识，不需要手工设计规则，只需要数据。Bahdanau 的注意力机制让这个承诺更加可信。

OpenAI 在 2015 年 12 月刚刚成立，由 Elon Musk、Sam Altman、Ilya Sutskever 等人发起，宣称要以 10 亿美元的承诺资金做"对全人类有益的 AI"。当时没有人能预料到，这个新成立的研究机构将在几年后推出 GPT 系列，彻底改变整个 AI 的格局。

学术界正在酝酿一个重要变化：NIPS（Neural Information Processing Systems，神经信息处理系统会议）——深度学习的"主会场"——的投稿量在 2014-2016 年出现了第一次爆发式增长，从 2014 年的约 1,700 篇飙升到 2016 年的 2,400 篇以上。深度学习不再只是"机器学习的一个子领域"——它正在变成整个 AI 的核心方法论。

→ 注意力机制让模型学会了"看哪里重要"，但它仍然被 RNN 的顺序计算所束缚——一个更激进的想法正在酝酿：如果抛弃一切循环结构，只用注意力来建模序列，会怎样？
