# 第 5 章 · 如何让机器记住过去——LSTM 与梯度消失

> "The problem was not with backpropagation. The problem was that backpropagation, when applied to recurrent networks, suffered from vanishing gradients." — Sepp Hochreiter

---

## Sepp Hochreiter 和 Jürgen Schmidhuber 是谁？

1997 年的慕尼黑，秋天的啤酒节刚刚结束，空气里还残留着烤香肠和啤酒花的味道。在慕尼黑工业大学（Technische Universität München）的一间朴素办公室里，一个年轻的博士生正在盯着屏幕上的一组数字发呆。他不是在写论文——他是在愤怒。

**Sepp Hochreiter**，1967 年出生在巴伐利亚的米尔多夫（Mühldorf am Inn）小镇。他的名字"Sepp"是巴伐利亚方言中"Joseph"的简称——典型的南德名字，朴实、接地气，就像他本人。Hochreiter 在慕尼黑工业大学读计算机科学，性格中有一种巴伐利亚人特有的倔强：认准了一件事，就会一条道走到黑。他的同学们大多在学编译器、数据库这些"好找工作"的方向，而 Hochreiter 却迷上了神经网络——一个在 1990 年代初被认为"已经被证明不行"的领域。

让 Hochreiter 着迷的是一个具体的问题：**循环神经网络**（Recurrent Neural Network, RNN）。前馈网络（就是我们前三章一直在讨论的那种）每次只看一个输入，算一次就完事了。但现实中很多数据是"有序列"的——一句话是一个词接一个词的序列，一段语音是一帧接一帧的序列，股票价格是一天接一天的序列。处理这种数据，网络需要一种"记忆"：看到当前输入的时候，还要记得之前的输入是什么。

RNN 的想法很简单：给网络加一个"循环连接"——把上一时刻的输出接到这一时刻的输入上。这样，信息就可以沿着时间流动了。1986 年，Rumelhart 等人（就是第 3 章发明反向传播的那几位）把反向传播算法扩展到了 RNN 上，叫做**沿时间反向传播**（Backpropagation Through Time, BPTT）。理论上，RNN 可以学习序列中任意长距离的依赖关系。

但理论上可以的事情，实际上经常不行。

Hochreiter 在实验中发现，RNN 在短序列上表现还行，一旦序列超过十几个时间步，训练就几乎完全失败了。不是学得慢——是根本学不到东西。梯度下降像是踩在了一块冰面上，怎么使劲都滑不向前。

Hochreiter 的导师告诉他，这大概只是实现上的问题，调调参数就好了。但 Hochreiter 不信。他花了整整一年时间，用数学仔细分析了 BPTT 中梯度到底是怎么传播的。最终他找到了病因——并在 1991 年的硕士论文中完整地写下了分析。这篇论文后来被认为是深度学习历史上最重要的理论分析之一。

但有一个问题：他需要一个博士导师来继续这项工作。

**Jürgen Schmidhuber** 1963 年出生在德国慕尼黑——和 Hochreiter 一样，也是南德人。Schmidhuber 的学术路径很不寻常：他在慕尼黑工业大学读了计算机科学，但他的真正热情在于一个当时极其小众的方向——用数学理解"学习"和"智能"的本质。1987 年获得学士学位后，他继续在慕尼黑工业大学攻读博士，1991 年获得博士学位。此后他先是留在慕尼黑工业大学做研究，后来转到瑞士的 IDSIA（Istituto Dalle Molle di Studi sull'Intelligenza Artificiale，瑞士人工智能实验室），并从 1995 年起担任 IDSIA 的负责人。

IDSIA 是一个有趣的地方。它坐落在瑞士南部意大利语区的卢加诺湖畔，由 Angelo Dalle Molle 基金会于 1988 年创立。Angelo Dalle Molle 是一位意大利企业家和慈善家，他在 1952 年发明了著名的苦味洋蓟利口酒 Cynar，后来将财富投入到提升人类生活质量的研究中，坚信人工智能会改变世界。整个 IDSIA 规模很小，只有十几位研究者，但 Schmidhuber 在这里度过了他最有创造力的时期。

Schmidhuber 的性格可以用两个字概括：**不服**。他不服神经网络"已经过时"的判断，不服 SVM 派"浅层就够了"的论调，不服主流学术界对深度学习的忽视。在整个 1990 年代和 2000 年代，当几乎所有人都在研究 SVM 和图模型的时候，Schmidhuber 一个人守在瑞士的湖边，和几个学生一起研究深度循环网络。

Hochreiter 在 1990 年代初进入慕尼黑工业大学攻读研究生，成为 Schmidhuber 的第一个学生——当时 Schmidhuber 还在慕尼黑工业大学任教。两人简直是天作之合——Hochreiter 有问题和分析，Schmidhuber 有坚持和方法。1997 年，他们联名发表了一篇论文：**"Long Short-Term Memory"**。这篇论文提出了一种全新的循环网络架构——LSTM，用精巧的"门控"机制解决了 RNN 的梯度消失问题。

这篇论文最初几乎没有引起任何注意。1997 年的 AI 界正在忙着庆祝 SVM 的胜利，没人关心两个德国人在瑞士湖边搞的循环网络。但二十年后，LSTM 成了 Google 翻译、Siri 语音助手背后的核心技术——直到被 Transformer 取代之前，它统治了序列建模长达二十年。

## 上一章的遗产：一个没解决的问题

第 4 章讲了 SVM 如何用严格的数学理论碾压了神经网络，成为 1990 年代的王者。但 SVM 有一个致命的盲区：它处理不了**序列**。

这里说的"序列"不是什么高深的概念。想想这些场景：你在读这句话的时候，每一个词的含义都依赖于前面的词——"我想吃一个苹果"和"苹果发布了新手机"中的"苹果"是完全不同的东西，你之所以知道这一点，是因为你"记得"前面的上下文。语音识别也是一样：同一个发音"shi"，在"老师"里是"师"，在"食物"里是"食"，你需要听前后的声音才能判断。

SVM 处理这种问题的方式是：把序列"压扁"成一个固定长度的向量，然后分类。比如把一整句话的所有词拼在一起，用一个巨大但固定的特征向量表示。这就像把一部电影的所有帧叠在一起，冲印成一张照片——信息损失巨大，时序关系全没了。

RNN 是为序列而生的。它的结构很简单：在每个时间步 $t$，网络接收当前输入 $\mathbf{x}_t$ 和上一时刻的隐藏状态 $\mathbf{h}_{t-1}$，计算新的隐藏状态：

$$\mathbf{h}_t = \sigma(W \mathbf{h}_{t-1} + U \mathbf{x}_t + \mathbf{b})$$

这里 $W$ 是隐藏状态到隐藏状态的权重矩阵，$\sigma$ 是某种激活函数（如 tanh）。信息沿着时间轴流动：$\mathbf{h}_1 \to \mathbf{h}_2 \to \cdots \to \mathbf{h}_T$。理论上，$\mathbf{h}_T$ "记得"从 $\mathbf{h}_1$ 开始的所有信息。

但理论是理论。让我们动手算一下，看看会发生什么。

## 核心洞察：一句话说清楚

**LSTM 的核心思想：如果信息是通过乘法传递的，它就会指数级衰减或爆炸；但如果我们在网络里修一条"信息高速公路"，让信息通过加法传递，它就能走很远。**

想象你在玩一个传话游戏：一排十个人，每个人把听到的消息传给下一个。如果规则是"每个人只能重复上一个人说的 80%"，那传到第十个人的时候，消息只剩 $0.8^{10} \approx 10\%$ 了——基本听不清了。这就是乘法传递的衰减。但如果你在队伍旁边修一条管道，消息通过管道以原始形态传送，每个人可以选择从管道里取消息还是从旁边的人那里听消息——这就不会衰减了。LSTM 里的"管道"就是细胞状态（cell state），而"选择取不取"就是门控（gating）。

## 数学原理

### 直觉解释

要理解 LSTM，我们得先理解 RNN 的病到底出在哪里。

RNN 的隐藏状态通过矩阵 $W$ 反复乘自己来传递：$\mathbf{h}_1$ 乘以 $W$ 得到 $\mathbf{h}_2$，再乘以 $W$ 得到 $\mathbf{h}_3$，依此类推。这就像你拿一张照片不断复印，每次复印都稍微失真一点——复印 20 次之后，图像可能已经模糊不清了（如果 $W$ 的特征值小于 1），也可能变成一团刺眼的白光（如果 $W$ 的特征值大于 1）。这就是**梯度消失和梯度爆炸**（vanishing/exploding gradients）。

反向传播也有同样的问题。误差信号要沿着时间轴"倒着"传回去，每传一步就要乘以 $W$。传了 50 步，就乘了 $W^{50}$。如果 $W$ 的特征值大部分小于 1，那 $W^{50}$ 的值趋近于零——远处的误差信号根本传不回来，网络"学不到"远距离的依赖关系。如果特征值大于 1，$W^{50}$ 会趋近于无穷大——训练直接崩溃。

Hochreiter 在 1991 年的硕士论文中第一个严格分析了这个问题。他证明了：只要激活函数是 sigmoid 或 tanh（它们的导数最大值为 1，通常更小），梯度消失就是不可避免的。这不是实现的问题，不是参数调得好就能解决的——它是 RNN 结构的内在缺陷。

LSTM 的解决方案堪称精妙。它引入了一个新的量——**细胞状态** $\mathbf{c}_t$。这个状态不是通过乘法更新的，而是通过加法更新的：

$$\mathbf{c}_t = \mathbf{f}_t \odot \mathbf{c}_{t-1} + \mathbf{i}_t \odot \tilde{\mathbf{c}}_t$$

这里 $\odot$ 是逐元素乘法（element-wise multiplication），$\mathbf{f}_t$ 是**遗忘门**（forget gate），$\mathbf{i}_t$ 是**输入门**（input gate），$\tilde{\mathbf{c}}_t$ 是候选新信息。关键在于第一项：$\mathbf{f}_t \odot \mathbf{c}_{t-1}$。当 $\mathbf{f}_t$ 接近 1 时，旧信息几乎原封不动地保留了——它是通过**近乎恒等的路径**传递的，不涉及矩阵乘法，所以梯度不会消失。这就是那条"信息高速公路"。

那三个"门"是什么？它们就是一些 sigmoid 函数，输出值在 0 到 1 之间——你可以把它们理解成水龙头：0 表示完全关闭，1 表示完全打开，0.7 表示打开 70%。遗忘门决定"旧记忆保留多少"，输入门决定"新信息写入多少"，输出门决定"从细胞状态里输出多少给外部"。

### 关键公式

**1. 遗忘门——决定忘掉什么：**

$$\mathbf{f}_t = \sigma(W_f \mathbf{h}_{t-1} + U_f \mathbf{x}_t + \mathbf{b}_f)$$

$\sigma$ 是 sigmoid 函数（$\sigma(z) = 1/(1+e^{-z})$），输出在 $(0,1)$ 之间。$\mathbf{f}_t$ 的每个分量是一个 0 到 1 的数，乘以旧的细胞状态——如果是 0，就"忘掉"那部分信息；如果是 1，就完全保留。

**2. 细胞状态更新——信息高速公路：**

$$\mathbf{c}_t = \underbrace{\mathbf{f}_t \odot \mathbf{c}_{t-1}}_{\text{保留旧信息}} + \underbrace{\mathbf{i}_t \odot \tilde{\mathbf{c}}_t}_{\text{写入新信息}}$$

这是 LSTM 最核心的公式。注意：旧信息是通过**逐元素乘法**（不是矩阵乘法！）保留的，当 $\mathbf{f}_t \approx 1$ 时，梯度对 $\mathbf{c}_{t-1}$ 的偏导数也接近 1——梯度可以几乎无损地流过。新信息 $\tilde{\mathbf{c}}_t = \tanh(W_c \mathbf{h}_{t-1} + U_c \mathbf{x}_t + \mathbf{b}_c)$ 通过输入门 $\mathbf{i}_t$ 控制"写入多少"。

**3. 输出门——决定输出什么：**

$$\mathbf{h}_t = \mathbf{o}_t \odot \tanh(\mathbf{c}_t)$$

$\mathbf{o}_t = \sigma(W_o \mathbf{h}_{t-1} + U_o \mathbf{x}_t + \mathbf{b}_o)$ 是输出门。细胞状态先通过 tanh 压缩到 $(-1,1)$，再由输出门决定哪些信息暴露给外部。

### 手算例子

**第一部分：感受梯度消失**

考虑最简单的 RNN（没有输入，只有循环）：$\mathbf{h}_t = W \mathbf{h}_{t-1}$，其中 $W$ 是一个标量（一维情况）。

- 如果 $W = 0.9$：经过 20 步后，$\mathbf{h}_{20} = 0.9^{20} \mathbf{h}_0 = 0.122 \mathbf{h}_0$——信号衰减到原来的 $12.2\%$。
- 如果 $W = 0.9$：经过 50 步后，$\mathbf{h}_{50} = 0.9^{50} \mathbf{h}_0 = 0.00515 \mathbf{h}_0$——几乎为零。
- 如果 $W = 1.1$：经过 50 步后，$\mathbf{h}_{50} = 1.1^{50} \mathbf{h}_0 = 117.4 \mathbf{h}_0$——信号爆炸了 117 倍。

在梯度下降中，反向传播的梯度也要乘以同样的因子。所以：要么远处的梯度几乎为零（学不到东西），要么远处的梯度变成天文数字（训练崩溃）。你几乎不可能恰好找到一组让梯度稳定流动的参数。

**第二部分：LSTM 的门控如何救命**

现在看 LSTM 的细胞状态更新（简化为一维）：$c_t = f_t \cdot c_{t-1} + i_t \cdot \tilde{c}_t$。

反向传播时，梯度对 $c_{t-1}$ 的偏导数是：$\frac{\partial c_t}{\partial c_{t-1}} = f_t$。

关键来了：**$f_t$ 是网络自己学出来的**，不是固定值。如果网络发现"我需要记住很久以前的信息"，它可以学会把 $f_t$ 设置为接近 1。此时：

- 经过 20 步：$c_{20} \approx 1^{20} c_0 = c_0$——信息完好无损！
- 经过 50 步：$c_{50} \approx 1^{50} c_0 = c_0$——依然完好！

让我们用一个具体数字走一遍。假设：

- 上一时刻细胞状态 $c_0 = 0.8$
- 当前输入 $x_1 = 1.0$，上一隐藏状态 $h_0 = 0.5$
- 所有权重都设为 0.5，偏置为 0（简化演示）

**遗忘门：** $f_1 = \sigma(0.5 \times 0.5 + 0.5 \times 1.0 + 0) = \sigma(0.75) = 0.68$

**输入门：** $i_1 = \sigma(0.5 \times 0.5 + 0.5 \times 1.0 + 0) = \sigma(0.75) = 0.68$

**候选信息：** $\tilde{c}_1 = \tanh(0.5 \times 0.5 + 0.5 \times 1.0 + 0) = \tanh(0.75) = 0.635$

**细胞状态更新：** $c_1 = 0.68 \times 0.8 + 0.68 \times 0.635 = 0.544 + 0.432 = 0.976$

**输出门：** $o_1 = \sigma(0.5 \times 0.5 + 0.5 \times 1.0 + 0) = \sigma(0.75) = 0.68$

**隐藏状态：** $h_1 = 0.68 \times \tanh(0.976) = 0.68 \times 0.753 = 0.512$

现在再走一步，假设新输入 $x_2 = 0$：

**遗忘门：** $f_2 = \sigma(0.5 \times 0.512 + 0.5 \times 0 + 0) = \sigma(0.256) = 0.564$

**输入门：** $i_2 = \sigma(0.5 \times 0.512 + 0.5 \times 0 + 0) = \sigma(0.256) = 0.564$

**候选信息：** $\tilde{c}_2 = \tanh(0.5 \times 0.512 + 0.5 \times 0 + 0) = \tanh(0.256) = 0.251$

**细胞状态：** $c_2 = 0.564 \times 0.976 + 0.564 \times 0.251 = 0.550 + 0.142 = 0.692$

注意：如果网络学会了把遗忘门调到接近 1（比如 $f_t = 0.99$），那 $c_2$ 就会接近 $0.976 + \text{新信息}$——旧信息几乎完全保留，新信息累加进来。梯度回传时，$\partial c_2 / \partial c_1 = f_2 \approx 0.99$，走 50 步也只是 $0.99^{50} \approx 0.61$——比普通 RNN 的 $0.9^{50} \approx 0.005$ 强了一百多倍！

## 这解决了一切吗？——新的局限

LSTM 是一个真正的突破。从 2000 年代中期开始，它逐渐成为序列建模的标准工具——语音识别、机器翻译、文本生成，到处都是 LSTM 的身影。2016 年 Google 翻译从统计方法切换到神经网络时，核心架构就是基于 LSTM 的编码器-解码器（encoder-decoder）模型。

但 LSTM 并不完美。

第一个问题是**速度**。LSTM 仍然是序列模型——你必须按顺序处理：先算 $t=1$，再算 $t=2$，再算 $t=3$，不能跳步，不能并行。当序列很长时（比如一段 1000 词的文本），这意味着至少 1000 步串行计算。在 GPU 这种天生为并行计算设计的硬件上，LSTM 的效率远不如卷积网络或全连接网络。

第二个问题是**记忆容量**。虽然 LSTM 缓解了梯度消失，但细胞状态仍然是一个固定大小的向量。如果你的序列有 10,000 个时间步，所有信息都要压缩到一个（比如）512 维的向量里——信息仍然会丢失，只是比普通 RNN 丢得慢一些。

第三个问题更根本：**LSTM 学会了"记住"，但没有学会"注意"**。在处理一个长句子时，LSTM 必须把所有信息塞进一个固定维度的向量，然后在需要的时候从这个向量里"提取"信息。但它没法在生成每个词的时候，回头去看源句子的不同位置——它只能依赖压缩后的向量，一个时间步接一个时间步地处理。

这三条局限性——速度慢、记忆有限、缺乏注意力——将成为下一代架构要解决的核心问题。而解决问题的起点，恰恰是 LSTM 催生的一种新架构：编码器-解码器模型。这个模型在翻译任务上取得了成功，但在长句子上暴露出了 LSTM 的瓶颈。正是为了解决这个瓶颈，研究者们发明了一种革命性的机制——注意力（attention）。

## 这一年的世界

1997 年，AI 领域的目光几乎完全不在循环神经网络上。那一年最轰动的 AI 事件是 **Deep Blue 击败了国际象棋世界冠军卡斯帕罗夫**——IBM 的超级计算机用蛮力搜索和精心设计的评估函数，在六局对抗中以 3.5:2.5 获胜。全世界都在讨论"机器是否已经超越了人类的智慧"，但行内人知道：Deep Blue 只是算得快，它不会"学习"任何东西。

与此同时，互联网泡沫正在膨胀。1997 年亚马逊刚刚上市，Google 还在斯坦福的宿舍里以一个研究项目的形式存在。全世界产生的文本数据正在呈指数增长——网页、邮件、新闻、论坛帖子——但谁来理解这些数据？谁来翻译这些跨越语言壁垒的海量信息？这些需求在当时看来还很遥远，但它们将成为推动序列建模发展的最大动力。

学术界的气氛则更加微妙。Schmidhuber 的 IDSIA 小组在卢加诺湖畔默默耕耘，几乎没有人注意到他们的工作。1997 年 LSTM 的论文发表在 *Neural Computation* 上——一份 respected 但小众的期刊。主流的机器学习会议（NeurIPS、ICML）上，绝大多数论文都在讨论 SVM 的变体、贝叶斯方法和图模型。一个典型的场景是：Schmidhuber 在会议上做报告，下面坐着几百个 SVM 的信徒，礼貌地鼓掌，然后继续讨论核函数的选择。

在地球另一端的多伦多大学，一个叫 Geoffrey Hinton 的教授也在关注这些问题。他还没有放弃神经网络——在所有人都不看好这个方向的时候，他一直在思考怎么让网络更深、更强。但他面对的敌人不是梯度消失，而是另一个问题：深网络太深了，根本训练不动。

这个问题的答案将在几年后到来，而且它的爆发方式，将超出所有人的想象。

→ LSTM 解决了梯度消失，让循环网络终于能处理长序列了。但序列模型天生只能一步步算，没法并行——当数据量越来越大、序列越来越长，这个"串行瓶颈"将变得越来越不可忍受。有没有一种方法，能彻底抛弃"一步一步来"的束缚？
