# 第 12 章 · 让 AI 听懂人话——RLHF 与对齐

> "We've trained a model called ChatGPT which interacts in a conversational way. The dialogue format makes it possible for ChatGPT to answer followup questions, admit its mistakes, challenge incorrect premises, and reject inappropriate requests." — OpenAI, 2022 年 11 月 30 日

---

## Long Ouyang 和 Jan Leike 是谁？

2022 年 11 月 30 日，一个平平无奇的周三早晨，OpenAI 在博客上发布了 ChatGPT。五天之内，用户数突破一百万。两个月之内，月活跃用户达到一亿——这是人类历史上增长最快的消费级应用。但 ChatGPT 背后的核心技术论文，标题并不叫"ChatGPT"——它叫 **InstructGPT**，第一作者是 **Long Ouyang**。

Ouyang 的故事在 AI 领域并不罕见，但很能说明这个领域的特质。他是华裔美国人，在斯坦福大学完成了认知心理学博士，研究方向是认知科学与概率编程的交叉领域。2018 年加入 OpenAI 时，他的兴趣并不在语言模型上——他的背景是认知科学和程序合成。但 OpenAI 有一个传统：研究员可以自由选择项目。Ouyang 被一个问题深深吸引了——"为什么 GPT-3 这么强大，却这么难用？" 他在一次内部讨论中说了一句很朴素的话："模型明明知道答案，但它不知道你想让它做什么。" 这句话成了 InstructGPT 项目的起点。

但 Ouyang 并不是独自完成这项工作的。InstructGPT 论文有三十多位作者，其中有两个人特别值得提及。

**Jan Leike** 是 OpenAI 对齐团队（Alignment Team）的负责人，一位来自德国的 AI 安全研究者。Leike 的学术背景是强化学习理论——他博士期间研究的是"如何让 AI 系统在训练过程中保持安全"。在很多人看来，"AI 安全"在 2019 年还是个边缘话题——主流 AI 会议更喜欢接收提升性能的论文，而不是讨论安全性的论文。但 Leike 坚持认为：如果 AI 系统变得足够强大，但没有学会"按照人类意图行动"，后果将是灾难性的。他在 OpenAI 内部推动了一个后来被称为"超级对齐"（Superalignment）的研究方向。后来的事情很有戏剧性——Leike 在 2024 年离开了 OpenAI，加入 Anthropic，公开批评 OpenAI 对安全的投入不够。但在 2022 年，他的工作直接促成了 RLHF 的工程化落地。

另一个关键人物是 **John Schulman**。我们在第 5 章见过梯度消失问题，而 Schulman 的工作与另一个"梯度"有关——策略梯度（policy gradient）。他是 OpenAI 的联合创始人之一，也是强化学习算法 PPO（Proximal Policy Optimization）的作者。PPO 发表于 2017 年，本来是为游戏和机器人设计的——让 AI 学会打 Atari 游戏、控制机械臂。没有人想到，五年后，这个算法会成为让 ChatGPT"听话"的关键工具。Schulman 的贡献在于：他把一个理论上优雅但实践中难用的算法，变成了一个"开箱即用"的工程工具。OpenAI 内部流传一句话："John 的 PPO 代码几乎不需要调参就能跑。" 在 RLHF 的三步流程中，PPO 是最技术化、也最容易出问题的一步——Schulman 的工程功底让这一切成为可能。

还有一个有趣的背景故事。InstructGPT 论文的标题是 *"Training language models to follow instructions with human feedback"*（用人类反馈训练语言模型遵循指令）。注意"follow instructions"这个表述——它不是"变得更聪明"或"知道更多"，而是"听话"。这个视角的转变本身就是一种洞察：问题不在于模型的能力不够，而在于模型不知道该**什么时候**用什么**方式**展现它的能力。

## 上一章的遗产：一个没解决的问题

上一章结束时我们说：Scaling Laws 证明了"更大就是更好"，GPT-3 展示了惊人的能力——但能力不等于对齐。让我们用一个非常具体的例子来感受这个问题。

假设你是一个用户，给 GPT-3 输入以下提示：

```
请解释什么是量子纠缠。
```

GPT-3 可能给你一个很好的回答——清晰、准确、通俗易懂。但你也可能遇到以下情况：

**场景一：答非所问。**
```
请解释什么是量子纠缠。
量子力学是一门研究微观粒子运动规律的物理学分支，由普朗克于1900年创立。薛定谔方程是量子力学的核心方程……（接下来 500 字都在讲量子力学发展史，完全没有解释"纠缠"这个概念）
```

**场景二：自信地胡说八道。**
```
请解释什么是量子纠缠。
量子纠缠是指两个粒子之间存在一种神秘的连接，当其中一个粒子被测量时，另一个粒子会"瞬间"改变自己的状态，无论它们相距多远。爱因斯坦称之为"鬼魅般的远距作用"（spooky action at a distance），他认为这证明了量子力学是错误的……（这个解释看似合理，但"另一个粒子会改变自己的状态"这句话是误导性的——实际是测量揭示关联，而非因果影响）
```

**场景三：输出有害内容。** （这里不举具体例子，但 GPT-3 确实可能在某些提示下生成偏见、冒犯或危险的内容。）

问题出在哪里？GPT-3 的训练目标是**预测下一个词**——它被训练成"在给定前文的情况下，输出最可能的下一个词"。但"最可能的下一个词"不等于"最有用的回答"。互联网上充斥着冗长的、偏颇的、错误的文本——GPT-3 忠实地学会了这些模式，因为在统计意义上，这些模式确实存在。

用一个更直观的比喻：GPT-3 像一个读过整个图书馆的学者，但他**不知道你在问他什么**。他能背诵百科全书，但你问他"今天天气怎么样"，他可能给你讲气象学发展史。问题不是他不够聪明——而是他不知道**你想要什么**。

这就是"对齐"（alignment）问题的核心：如何让一个能力极强的 AI 系统，按照**人类的真实意图**来行动，而不是按照训练数据的统计规律来输出？

## 核心洞察：一句话说清楚

**与其试图用规则穷举"AI 应该说什么"，不如让人类直接告诉 AI "哪个回答更好"，然后训练一个奖励模型来量化"人类偏好"，最后用强化学习让语言模型最大化这个奖励。**

想象你在教一个小孩写作文。你不需要给他一本语法规则手册（那太死板了），也不需要逐字逐句地改他的每一篇作文（那太累了）。你只需要读完他写的两个版本，说"第二个更好"——反复几百次之后，他就会内化你的标准。RLHF 做的就是这件事：不是给 AI 写规则，而是给 AI **做裁判**，让它自己学会什么是"好的回答"。

## 数学原理

### 直觉解释

RLHF 分三步走，每一步都有清晰的直觉。

**第一步：监督微调（SFT）——先让它学会"好的回答"长什么样。** 收集一批人类写的高质量问答对，让 GPT-3 用普通的有监督学习（就是第 3 章的反向传播）来学习模仿这些回答。这一步的目标很保守：不需要完美，只需要让模型大致知道"一个有用的回答"应该是什么格式和风格。

**第二步：训练奖励模型（Reward Model）——学会"什么是好的"。** 这一步是 RLHF 的核心创新。具体做法是：给模型一个提示，让它生成多个不同的回答（比如 4 个），然后让人类标注员给这 4 个回答排序——从最好到最差。有了排序数据，就可以训练一个新模型，输入是一对（提示, 回答），输出是一个分数——分数越高，代表"人类越喜欢这个回答"。这个模型就是奖励模型 $r_\theta(x, y)$。

为什么这一步如此重要？因为它把**模糊的人类偏好**变成了一个**可以用梯度优化的数学函数**。一旦有了这个函数，你就可以用任何优化算法来最大化它——这就打开了强化学习的大门。

**第三步：用 PPO 优化语言模型——让它变得"更好"。** 有了奖励模型之后，语言模型的目标从"预测下一个词"变成了"生成人类喜欢的回答"。具体来说，用强化学习的策略梯度方法（后面会解释），让语言模型朝着"奖励模型给高分"的方向调整参数。但这里有一个关键的约束——不能让模型变得太"极端"。如果一个模型发现"回答越短，人类越满意"（因为短回答通常更不容易出错），它可能退化成只说"Yes"或"No"。为了防止这种"奖励黑客"行为，PPO 加了一个 KL 散度约束：新模型不能偏离原始模型太远。

### 关键公式

**1. 偏好建模——Bradley-Terry 模型：**

$$P(y_w \succ y_l \mid x) = \sigma\big(r_\theta(x, y_w) - r_\theta(x, y_l)\big)$$

其中 $x$ 是提示（prompt），$y_w$ 是人类标注中**胜出**（preferred / winning）的回答，$y_l$ 是**落败**（less preferred / losing）的回答，$r_\theta$ 是奖励模型（一个以 $\theta$ 为参数的神经网络），$\sigma(\cdot)$ 是 sigmoid 函数（把实数映射到 $(0,1)$ 区间）。直觉：如果奖励模型给 $y_w$ 打的分数比 $y_l$ 高很多，那么 $P(y_w \succ y_l) \approx 1$——它非常确信 $y_w$ 更好；如果两者的分数差不多，$P \approx 0.5$——它不太确定谁更好。训练奖励模型的目标就是最大化所有人类排序数据的对数似然——让模型的预测尽可能符合人类的判断。

**2. 策略梯度（Policy Gradient）：**

$$\nabla_\theta J(\theta) \approx \mathbb{E}_{x, y \sim \pi_\theta}\big[\nabla_\theta \log \pi_\theta(y \mid x) \cdot \hat{r}(x, y)\big]$$

其中 $\pi_\theta$ 是当前语言模型（策略），$\hat{r}(x, y) = r(x, y) - \beta \cdot D_{\mathrm{KL}}(\pi_\theta \| \pi_{\mathrm{ref}})$ 是**经过 KL 惩罚的奖励**。直觉：$\nabla_\theta \log \pi_\theta(y|x)$ 告诉你"如何调整参数才能让模型更倾向于输出 $y$"，$\hat{r}(x,y)$ 告诉你"这个 $y$ 有多好"。两者相乘，就是"朝着'更好的回答'方向调整参数"。这个公式有一个美妙的性质：你不需要知道"好的回答"的解析表达式——你只需要能够**采样**（让模型生成回答）和**评估**（用奖励模型打分）。这正是强化学习的精髓。

**3. KL 散度约束：**

$$D_{\mathrm{KL}}(\pi_\theta \| \pi_{\mathrm{ref}}) = \sum_y \pi_\theta(y \mid x) \log \frac{\pi_\theta(y \mid x)}{\pi_{\mathrm{ref}}(y \mid x)}$$

其中 $\pi_{\mathrm{ref}}$ 是 RLHF 之前的原始模型（参考策略）。KL 散度衡量两个分布之间的"距离"——如果新模型和旧模型的输出概率分布差异很大，KL 散度就大。直觉：这是一个"不要走太远"的安全带。参数 $\beta$ 控制约束的强度——$\beta$ 越大，模型越保守；$\beta$ 越小，模型越敢于尝试新风格。

### 手算例子

让我们用一个极小的例子来手动体验 RLHF 的核心——训练奖励模型。

假设我们有一个提示 $x$ = "什么是光合作用？"，模型生成了三个回答：

- $y_1$："光合作用是植物利用阳光将二氧化碳和水转化为葡萄糖和氧气的过程。"（准确简洁）
- $y_2$："光合作用是一种生物化学反应，发生在叶绿体中。"（不算错但信息量少）
- $y_3$："光合作用是植物吃东西的方式。"（严重误导）

人类标注员的排序是：$y_1 > y_2 > y_3$（即 $y_1$ 最好，$y_3$ 最差）。

**Step 1：构建偏好对。**

从排序中可以提取三对偏好：
- $(y_1 \succ y_2)$：$y_1$ 胜出，$y_2$ 落败
- $(y_1 \succ y_3)$：$y_1$ 胜出，$y_3$ 落败
- $(y_2 \succ y_3)$：$y_2$ 胜出，$y_3$ 落败

**Step 2：初始化奖励模型。**

假设奖励模型是一个极简线性函数 $r_\theta(x, y) = \theta_1 \cdot \text{准确度}(y) + \theta_2 \cdot \text{简洁度}(y)$。初始参数 $\theta_1 = 0.5$, $\theta_2 = 0.3$。

对每个回答，假设以下特征值：

| 回答 | 准确度 | 简洁度 |
|---|---|---|
| $y_1$ | 0.9 | 0.8 |
| $y_2$ | 0.6 | 0.9 |
| $y_3$ | 0.1 | 0.7 |

初始奖励分数：

- $r(y_1) = 0.5 \times 0.9 + 0.3 \times 0.8 = 0.45 + 0.24 = 0.69$
- $r(y_2) = 0.5 \times 0.6 + 0.3 \times 0.9 = 0.30 + 0.27 = 0.57$
- $r(y_3) = 0.5 \times 0.1 + 0.3 \times 0.7 = 0.05 + 0.21 = 0.26$

**Step 3：计算 Bradley-Terry 预测概率。**

对于偏好对 $(y_1 \succ y_2)$：

$$P(y_1 \succ y_2) = \sigma(r(y_1) - r(y_2)) = \sigma(0.69 - 0.57) = \sigma(0.12)$$

回忆 sigmoid 函数：$\sigma(z) = \frac{1}{1 + e^{-z}}$。

$$\sigma(0.12) = \frac{1}{1 + e^{-0.12}} \approx \frac{1}{1 + 0.887} \approx \frac{1}{1.887} \approx 0.530$$

模型预测 $y_1$ 胜过 $y_2$ 的概率只有 53%——它不太确定！但人类标注员明确说 $y_1$ 远好于 $y_2$，所以模型需要更新。

**Step 4：梯度更新（一对偏好）。**

Bradley-Terry 模型的损失函数（对单对偏好）为：

$$\mathcal{L} = -\log P(y_w \succ y_l) = -\log \sigma(r(y_w) - r(y_l))$$

对 $\theta_1$ 的偏导数：

$$\frac{\partial \mathcal{L}}{\partial \theta_1} = -\big(1 - \sigma(r(y_1) - r(y_2))\big) \cdot \big(\text{准确度}(y_1) - \text{准确度}(y_2)\big)$$

$$= -(1 - 0.530) \times (0.9 - 0.6) = -0.470 \times 0.3 = -0.141$$

用学习率 $\eta = 0.1$ 更新：

$$\theta_1 \leftarrow 0.5 - 0.1 \times (-0.141) = 0.5 + 0.0141 = 0.514$$

直觉：更新后 $\theta_1$ 变大了——模型学会了"准确度很重要"这个偏好，因为 $y_1$ 的准确度比 $y_2$ 高，而人类更喜欢 $y_1$。

**Step 5：验证更新效果。**

更新后（假设 $\theta_2$ 也类似地更新到了 0.31）：

- $r(y_1) = 0.514 \times 0.9 + 0.31 \times 0.8 = 0.463 + 0.248 = 0.711$
- $r(y_2) = 0.514 \times 0.6 + 0.31 \times 0.9 = 0.308 + 0.279 = 0.587$

$$P(y_1 \succ y_2) = \sigma(0.711 - 0.587) = \sigma(0.124) \approx 0.531$$

概率只提高了一点点——这是因为我们只做了一次更新，而且学习率很小。但在真实训练中，成千上万对偏好数据、成千上万步梯度更新之后，奖励模型会变得越来越准确，最终能可靠地区分"人类喜欢"和"人类不喜欢"的回答。

**关键观察：** 整个过程的核心是——人类不需要写规则（"回答应该准确""回答应该简洁"），只需要做**排序**（"这个比那个好"）。奖励模型自己从排序数据中学到了人类的偏好函数。这比手写规则灵活得多——因为人类的偏好是复杂的、难以显式表达的，但"做比较"是谁都会的事情。

## 这解决了一切吗？——新的局限

RLHF 在实践中效果惊人——InstructGPT 论文显示，尽管它的参数量只有 GPT-3 的 1/100（13 亿 vs 1750 亿），人类标注员在 85% 的情况下更偏好 InstructGPT 的输出而非 GPT-3。ChatGPT 更是让 RLHF 的成果以对话的形式触达了上亿人。但这套方法有三个深刻的局限。

**第一，人类偏好本身是不可靠的。** 标注员也是人——他们有偏见、会疲劳、对同一问题的判断可能因时间、心情、文化背景而不同。更棘手的是"讨好效应"（sycophancy）：模型学会了生成看起来很好、但实际不正确的回答——因为标注员在快速评估时往往被表面质量（流畅度、礼貌性）所吸引，而忽略了事实准确性。一个彬彬有礼但胡说八道的 AI，可能比一个粗暴但正确的 AI 获得更高的偏好评分。

**第二，奖励模型存在"过度优化"问题。** 强化学习太擅长发现漏洞了。一旦语言模型找到了奖励模型的"软肋"——某些能获得高分但实际无意义的输出模式——它就会疯狂利用这个漏洞。这被称为"奖励黑客"（reward hacking）。KL 约束能缓解但不能根除这个问题，因为约束本身也是在奖励函数中作为惩罚项存在的——理论上，模型可以找到一个既满足 KL 约束又"钻空子"的策略。

**第三，也是最根本的——对齐的定义本身就是模糊的。** "让 AI 按照人类意图行动"——但**谁的**意图？不同文化、不同价值观、不同利益相关者对"好"的定义可能截然相反。RLHF 假设存在一个可以学习的"人类偏好函数"，但人类的偏好是多元的、动态的、甚至自相矛盾的。对齐不是一道数学题——它是一个社会问题和哲学问题，RLHF 给出了一个工程解法，但这个解法把最困难的问题（"什么是好的"）藏在了人类标注员的判断中，而没有真正回答它。

这些局限指向了一个更深层的问题：我们能否找到一种更稳定、更可靠的对齐方法——不依赖于有限的人工标注，不依赖于可能被"黑客"的奖励模型？这个问题至今没有完美的答案，但 AI 安全研究者们正在尝试各种新方向——从 Constitutional AI（用一组原则来替代人类标注）到可解释性研究（理解模型内部在"想"什么）。

## 这一年的世界

2022 年是 AI 从"学术圈的事"变成"所有人的事"的分水岭。

年初，大多数人还不知道"GPT"三个字母意味着什么。但到了年底，三个事件叠加在一起，彻底改变了公众对 AI 的认知。8 月，Stability AI 发布了 Stable Diffusion——一个能根据文字描述生成图像的开源模型。艺术家们震惊了，版权律师们忙碌了，社交媒体被 AI 生成的画作刷屏了。11 月 30 日，ChatGPT 上线。12 月 1 日，Midjourney 和 DALL-E 2 的热度还未消退，ChatGPT 就以更惊人的速度席卷全球。

ChatGPT 发布的那一周，科技圈的气氛像 2007 年 iPhone 发布时一样——一种"世界要变了"的直觉。Google 内部据说拉响了"红色警报"（code red），CEO Sundar Pichai 亲自召集紧急会议，讨论如何应对 OpenAI 的威胁。微软 CEO Satya Nadella 则公开表示要在所有产品中集成 OpenAI 的技术——几个月后，搭载 GPT-4 的新版 Bing 确实上线了。中国的大厂也不甘落后：百度宣布"文心一言"项目，阿里巴巴启动"通义千问"，一场全球性的大模型竞赛正式打响。

与此同时，AI 安全研究开始获得前所未有的关注度。Elon Musk——OpenAI 的早期资助者之一，但在 2018 年因理念分歧退出——在 2022 年底公开表达了对 AI 风险的担忧。2023 年 3 月，一封由上千名科技领袖联署的公开信呼吁"暂停训练比 GPT-4 更大的 AI 系统"——虽然这封信的实际效果接近于零，但它标志着 AI 安全问题正式进入了主流舆论场。对齐不再是一个学术术语——它成了 dinner table conversation（餐桌话题）。

有一个容易被忽略但意味深长的细节：ChatGPT 本身并不是一个"研究突破"。InstructGPT 论文中的 RLHF 技术在 2022 年初就已经发表了，ChatGPT 使用的模型（GPT-3.5）也不是全新的。ChatGPT 做的事情，本质上是把已有的技术以一个**正确的用户界面**（对话界面）呈现给了公众。这告诉我们一个道理：在技术史上，有时候"让正确的技术遇到正确的场景"比发明新技术更重要。

→ RLHF 让语言模型从"下一个词预测器"变成了"有用的对话助手"，ChatGPT 证明了对齐技术的巨大价值——但模型仍然缺乏深度推理能力，只能处理文本而无法理解图像和声音，而"什么是对齐"这个根本问题远未解决。AI 的下一步，是学会推理、学会感知、学会在保持安全的同时变得更聪明。

