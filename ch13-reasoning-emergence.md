# 第 13 章 · 推理、融合与涌现——当前前沿

> "We've discovered that larger models don't just do a little better at things they already know—they suddenly gain entirely new abilities that smaller models completely lack." — Jason Wei et al., 2022

---

## Jason Wei 是谁？

如果你关注 AI 领域，你大概率听过"Chain-of-Thought"（思维链）这个词。这个词的缔造者叫 **Jason Wei**，2022 年初发表论文时，他还是 Google Research 的一名研究员——一个刚从大学毕业没几年的年轻人。

Wei 的学术背景并不算"正统 AI"。他本科在达特茅斯学院（Dartmouth）读计算机科学，期间对自然语言处理产生了兴趣。他的特殊之处在于一种极好的研究品味——不是那种"在已有方法上刷两个百分点"的品味，而是"问一个所有人都能理解但没人想到去问的问题"的品味。在 Google 内部，他的同事们说他是那种"能用一句话解释清楚一个复杂想法"的人。

2022 年初，大语言模型（GPT-3、PaLM 等）已经在各种任务上展现了强大的能力，但在一个领域表现一直很差：**多步推理**。如果你问 GPT-3"一个商店有 23 个苹果，早上卖了 12 个，下午又进货 15 个，现在有几个？"，它很可能答错。奇怪的是，如果你在提问之前先给它几个类似的题目，并且**把解题过程一步步写出来**（而不是只给答案），它突然就能做对了。

这个现象很多人注意到了，但 Wei 是第一个把它系统化、量化、并且用大量实验证明其普遍性的人。2022 年 1 月，他和合作者在 arXiv 上发表了 *"Chain-of-Thought Prompting Elicits Reasoning in Large Language Models"*。论文的核心发现用一句话概括就是：**让模型"把思考过程说出来"，它的推理能力就会大幅提升。**

这个发现看似简单，实际上揭示了一个深刻的现象——**涌现**（emergence）。Wei 在同一时期的另一篇论文中发现：很多能力（包括思维链推理）在小模型上完全不存在，但当模型规模超过某个阈值时，这些能力会突然出现。这不是"渐进变好"——而是"从无到有"的质变。这个发现让"把模型做大"这件事从经验直觉变成了有科学依据的策略。

但本章的主角不止一个人。在推理之外，还有两条平行的技术线索正在重塑 AI 的能力边界。

**多模态对齐**方面，Alec Radford 领导的团队（OpenAI）在 2021 年发布了 **CLIP**（Contrastive Language-Image Pre-training），这是一个让语言和视觉"说同一种语言"的模型。它的思路极其巧妙：不需要人工标注图像内容，只需从互联网上抓取 billions 级的"图片-描述"配对，训练模型学会"这张图片和这段文字描述的是同一件事"。到 2023-2024 年，这条路线催生了 GPT-4V、Gemini 等能"看图说话"的多模态模型。

**混合专家模型**（Mixture of Experts, MoE）方面，Google 的 **Barret Zoph** 和 **William Fedus** 等人在 2021-2022 年将这个 1990 年代的旧想法（对，又是老想法）重新包装并成功应用于 Transformer。MoE 的核心思想是：与其让所有参数同时工作（太贵了），不如让模型学会**按需选择**——每个输入只激活一小部分"专家"，其余参数保持沉默。这让你可以在不增加实际计算量的情况下，把模型参数量扩大到万亿级别。到 2024 年，Mixtral、DeepSeek-V3、GPT-4 等模型都采用了 MoE 或类似的稀疏架构。

## 上一章的遗产：一个没解决的问题

上一章结束时，我们留下了三个问题。让我们用具体的例子来感受最紧迫的那个。

假设你问一个经过 RLHF 训练的 ChatGPT（2022 年底的版本）：

```
小明有 5 个苹果，给了小红 2 个。然后小明又买了 3 个苹果。
请问小明现在有几个苹果？
```

模型很可能正确回答"6 个"。但如果你稍微加一步：

```
小明有 5 个苹果，给了小红 2 个。然后小明又买了 3 个苹果，
但其中 1 个是坏的所以扔掉了。小红的苹果比小明少 1 个。
请问小红有几个苹果？
```

模型的回答大概率是错的。为什么？因为 RLHF 训练的是"生成人类喜欢的回答"，而不是"一步一步严格推理"。模型学会了流畅表达、礼貌回答、遵循指令——但当需要多步逻辑推导时，它就像一个聪明但粗心的学生：直觉很好，但不打草稿，一步算错后面全崩。

这暴露了一个根本问题：RLHF 让模型变得"听话"，但没有让模型变得"会想"。模型的推理能力主要来自预训练阶段（预测下一个词）——但在预训练中，模型学到的是**模仿**人类写出的推理过程，而不是**真正执行**推理。它更像是一个背下了大量解题格式的学生，而不是一个理解了数学原理的学生。

与此同时，模型还有两个明显的"感官缺陷"：它只能处理文字。你给它看一张照片问"这是哪里？"，它无能为力。你给它放一段音乐问"这是什么曲子？"，它完全不知道。一个只能读文字的 AI，不管多聪明，都像是一个被蒙住眼睛、捂住耳朵的人。

另外，Scaling Laws 告诉我们"越大越好"，但"大"是有物理极限的——训练一个 GPT-4 级别的模型已经需要数万块 GPU 运行数月，耗资上亿美元。如果继续等比例放大，下一代模型可能需要一个小城市的电力。有没有办法让模型变强，但不按比例增加计算成本？

## 核心洞察：一句话说清楚

**与其让模型直接给出答案，不如教它"先打草稿再写答案"；与其让模型越来越大，不如让它在每次推理时只激活最相关的部分；与其只喂文字，不如把所有信息都翻译成同一种"语言"——向量。**

想象一个大学考试的场景。如果一道复杂的数学题要求你直接写出最终答案——不许写中间步骤——你大概率会出错。但如果允许你在草稿纸上一步步推导，你的准确率会高得多。这不是因为你变聪明了，而是因为**把一个复杂问题分解成多个简单步骤**，每一步都更容易做对。思维链做的就是这件事：让模型的"内心独白"外化，变成可见的推理步骤。

而 MoE 就像一个大型咨询公司。公司有 100 位顾问（专家），但每个客户问题只派 5 位最相关的顾问去处理——既省时间又省成本。客户的请求不需要所有 100 位顾问都参与讨论，只需要找到最对口的几位就行了。

## 数学原理

### 直觉解释

本章涉及三个方向，让我们逐一用直觉理解。

**涌现能力。** 想象你在烧水。温度从 $20°\text{C}$ 升到 $90°\text{C}$，水看起来没什么变化——还是液态。但当温度突破 $100°\text{C}$ 时，突然冒出大量气泡，水变成了蒸汽。这个转变不是渐进的——它是一个"相变"（phase transition）。AI 模型的能力增长也有类似的模式：很多能力在小模型上完全不存在，当模型规模超过某个阈值时，这些能力会突然"冒出来"。这不是某个参数被"激活"了，而是一种统计效应——当模型的参数量和训练数据多到一定程度时，它自然而然地获得了整合多种模式的能力。

**混合专家模型（MoE）。** 想象一个医院。如果每个病人来了都要所有的医生一起会诊——骨科、皮肤科、精神科、妇产科——那效率极低。现实中，分诊台（路由函数）会根据症状把病人分配给最相关的两个科室。MoE 中的"路由函数"就是这个分诊台：它看一眼输入，然后决定"这个问题最适合由哪几个'专家'来处理"。每个专家本质上就是一个独立的前馈神经网络（全连接层），但整个层只有被选中的专家会进行计算。

**多模态对齐。** 想象你参加一个国际会议，参会者来自世界各地，语言不通。但如果每个人都能把自己的语言翻译成一种通用语言（比如数学），大家就能交流了。CLIP 做的就是这种"翻译"：它训练两个编码器——一个把图片编码成向量，一个把文字编码成向量——然后用对比学习让"匹配的图文对"的向量靠近、"不匹配的图文对"的向量远离。训练完成后，同一个向量空间里既能表示文字的含义，也能表示图片的内容——"一张猫的照片"和"一只毛茸茸的猫"的向量会非常接近。

### 关键公式

**1. MoE 路由函数（稀疏门控）：**

$$g(x) = \text{softmax}\big(\text{TopK}(W_g \cdot x)\big)$$

其中 $x$ 是输入向量，$W_g$ 是路由权重矩阵（形状为 $E \times d$，$E$ 是专家数量，$d$ 是向量维度），$\text{TopK}(\cdot)$ 只保留最大的 $K$ 个值、将其余设为 $-\infty$（softmax 后变成 0），$g(x)$ 输出一个长度为 $E$ 的稀疏向量——只有 $K$ 个位置非零。最终的 MoE 层输出为：

$$\text{MoE}(x) = \sum_{i=1}^{E} g(x)_i \cdot E_i(x)$$

其中 $E_i(x)$ 是第 $i$ 个专家对 $x$ 的输出。因为 $g(x)$ 中只有 $K$ 个位置非零，所以实际上只计算了 $K$ 个专家——其余 $E-K$ 个专家的计算被完全跳过。

**2. CLIP 对比学习损失：**

$$\mathcal{L} = -\frac{1}{N}\sum_{i=1}^{N}\left[\log\frac{\exp(\text{sim}(v_i, t_i)/\tau)}{\sum_{j=1}^{N}\exp(\text{sim}(v_i, t_j)/\tau)} + \log\frac{\exp(\text{sim}(t_i, v_i)/\tau)}{\sum_{j=1}^{N}\exp(\text{sim}(t_j, v_i)/\tau)}\right]$$

其中 $v_i$ 是第 $i$ 张图片的向量编码，$t_i$ 是第 $i$ 段文字的向量编码，$\text{sim}(\cdot,\cdot)$ 是余弦相似度，$\tau$ 是温度参数，$N$ 是一个 batch 中的图文对数量。直觉：对于每张图片 $v_i$，让它和自己配对的文字 $t_i$ 相似度最高（分子），同时让它和其他 $N-1$ 段不配对的文字 $t_j$ 的相似度尽量低（分母）。反之亦然——每段文字也要找到自己配对的图片。这就像一个"配对考试"：$N$ 对正确配对混在 $N \times N$ 种可能配对中，模型要学会把正确的那对挑出来。

### 手算例子

让我们手动走一遍 MoE 的路由过程。假设一个 MoE 层有 4 个专家，每次只激活 Top-2。

**设定。** 输入向量 $x = \begin{pmatrix} 1.0 \\ 0.5 \end{pmatrix}$（2 维），路由权重矩阵：

$$W_g = \begin{pmatrix} 0.8 & 0.3 \\ 0.2 & 0.9 \\ 0.7 & 0.1 \\ 0.4 & 0.6 \end{pmatrix}$$

每行对应一个专家。4 个专家各自有一个简单的线性变换（这里假设都是 $2 \times 2$ 矩阵乘法后接 ReLU）：

$$E_1: W_1 = \begin{pmatrix} 0.5 & 0.2 \\ 0.1 & 0.8 \end{pmatrix}, \quad E_2: W_2 = \begin{pmatrix} 0.3 & 0.7 \\ 0.6 & 0.2 \end{pmatrix}$$

$$E_3: W_3 = \begin{pmatrix} 0.9 & 0.1 \\ 0.3 & 0.4 \end{pmatrix}, \quad E_4: W_4 = \begin{pmatrix} 0.2 & 0.5 \\ 0.7 & 0.3 \end{pmatrix}$$

**Step 1：计算路由分数。**

$$W_g \cdot x = \begin{pmatrix} 0.8 \times 1.0 + 0.3 \times 0.5 \\ 0.2 \times 1.0 + 0.9 \times 0.5 \\ 0.7 \times 1.0 + 0.1 \times 0.5 \\ 0.4 \times 1.0 + 0.6 \times 0.5 \end{pmatrix} = \begin{pmatrix} 0.95 \\ 0.65 \\ 0.75 \\ 0.70 \end{pmatrix}$$

**Step 2：选择 Top-2。**

最大的两个值：$0.95$（专家 1）和 $0.75$（专家 3）。保留这两个，其余设为 $-\infty$：

$$\text{TopK}(W_g \cdot x) = \begin{pmatrix} 0.95 \\ -\infty \\ 0.75 \\ -\infty \end{pmatrix}$$

**Step 3：Softmax 归一化。**

$$g(x) = \text{softmax}\begin{pmatrix} 0.95 \\ -\infty \\ 0.75 \\ -\infty \end{pmatrix}$$

$-\infty$ 经过 softmax 后变成 $e^{-\infty} = 0$，所以只需对 0.95 和 0.75 计算：

$$e^{0.95} \approx 2.586, \quad e^{0.75} \approx 2.117, \quad \text{总和} = 4.703$$

$$g(x) = \begin{pmatrix} 2.586 / 4.703 \\ 0 \\ 2.117 / 4.703 \\ 0 \end{pmatrix} = \begin{pmatrix} 0.550 \\ 0 \\ 0.450 \\ 0 \end{pmatrix}$$

专家 1 获得了 55% 的权重，专家 3 获得了 45% 的权重。专家 2 和专家 4 完全不参与计算。

**Step 4：计算被激活专家的输出。**

专家 1 的输出：$E_1(x) = \text{ReLU}(W_1 \cdot x) = \text{ReLU}\begin{pmatrix} 0.5 \times 1.0 + 0.2 \times 0.5 \\ 0.1 \times 1.0 + 0.8 \times 0.5 \end{pmatrix} = \text{ReLU}\begin{pmatrix} 0.60 \\ 0.50 \end{pmatrix} = \begin{pmatrix} 0.60 \\ 0.50 \end{pmatrix}$

专家 3 的输出：$E_3(x) = \text{ReLU}(W_3 \cdot x) = \text{ReLU}\begin{pmatrix} 0.9 \times 1.0 + 0.1 \times 0.5 \\ 0.3 \times 1.0 + 0.4 \times 0.5 \end{pmatrix} = \text{ReLU}\begin{pmatrix} 0.95 \\ 0.50 \end{pmatrix} = \begin{pmatrix} 0.95 \\ 0.50 \end{pmatrix}$

专家 2 和专家 4 的输出：**不计算**（这是 MoE 节省算力的关键）。

**Step 5：加权求和得到最终输出。**

$$\text{MoE}(x) = 0.550 \times \begin{pmatrix} 0.60 \\ 0.50 \end{pmatrix} + 0.450 \times \begin{pmatrix} 0.95 \\ 0.50 \end{pmatrix}$$

$$= \begin{pmatrix} 0.330 \\ 0.275 \end{pmatrix} + \begin{pmatrix} 0.428 \\ 0.225 \end{pmatrix} = \begin{pmatrix} 0.758 \\ 0.500 \end{pmatrix}$$

**关键对比：** 一个标准全连接层需要计算 1 个 $4 \times 2$ 矩阵乘法（8 次乘法）。我们的 MoE 有 4 个专家，但只计算了 2 个（4 次矩阵乘法），然后用路由权重加权平均。当专家数量从 4 扩展到 64 或 128 时（现实中常见的规模），节省的比例更加显著——参数量是全连接的 64 倍，但每次推理只多花了 2 倍的计算量。这就是 MoE 的"以参数换性能"策略：用更多的**存储**（参数），换取更好的**效果**，而不按比例增加**计算**。

## 这解决了一切吗？——新的局限

这三个方向——推理、多模态、稀疏架构——在 2023-2025 年取得了惊人的进展。思维链让模型在数学竞赛上的得分从"不及格"跃升到"接近金牌"；多模态让模型能看懂图片、分析视频、甚至理解图表；MoE 让万亿参数模型在消费级硬件上运行。但每一项进步都带来了新的困惑。

**涌现的本质仍然是个谜。** 我们观察到"模型大到一定程度就突然会做某件事了"，但**为什么**？涌现是真正的能力质变，还是统计假象？2023 年的一项研究（Schaeffer, Miranda & Koyejo）指出，某些"涌现"可能是评估指标造成的错觉——如果用连续指标（而非"对/错"的硬指标）来衡量，同样的能力可能是渐进增长的，而不是突然出现的。更深层的问题是：我们能否**预测**模型在什么规模下会获得什么能力？如果不能预测，那"把模型做大"就仍然是一种"烧香拜佛"式的策略。

**推理的"思考"真的是思考吗？** 思维链让模型表现更好，但这是否意味着模型在"推理"？一个更可能的解释是：思维链增加了"推理步数"——更长的生成序列意味着更多的前向传播（forward pass），给了模型更多的"计算时间"来整合信息。2024 年的推理模型（如 OpenAI o1、DeepSeek-R1）进一步强化了这个方向：它们在给出最终答案前，会先生成一段很长的"内部思考链"（有些模型甚至把这叫"思考 token"）。但这些"思考"是否等价于人类的逻辑推理，还是一种更高级的模式匹配？这个问题的答案目前没人知道。

**多模态对齐的"对齐"有多深？** CLIP 让图片和文字进入了同一个向量空间，但这不意味着模型"理解"了图片——它可能只是学会了"什么样的文字描述和什么样的图片像素模式经常一起出现"。一个经典的反例：CLIP 可以把"一张医生的图片"和文字"医生"匹配起来，但它可能同时把"白人男性"和"医生"的关联度设得很高——因为它从训练数据中学到了这种统计偏差。多模态不是万能药——它让模型获得了新的"感官"，但也继承了训练数据中所有的偏见。

**最根本的问题是：我们正接近一个认知边界。** 从第 1 章的感知机到现在的多模态推理模型，我们一路走来，每一次进步都伴随着新的困惑。但这一次不同——以前的困惑是"我们不知道怎么让模型做到 X"，现在的困惑是"模型做到了 X，但我们不知道它是怎么做到的"。我们对万亿参数模型内部运作机制的理解，可能并不比对人脑的理解多多少。我们建造了一座越来越精密的钟，但仍然不知道钟表匠是谁。

## 这一年的世界

2023-2025 年，AI 已经不再是科技新闻——它成了所有新闻。

2023 年 2 月，Meta 发布 LLaMA 并很快被泄露到网上，催生了整个开源大模型社区。3 月，GPT-4 发布——它不仅比 GPT-3.5 更聪明，而且首次具备了看图能力——你可以给它一张冰箱里的照片，问"我能做什么菜？"，它会分析食材然后给出建议。同月，一封由 Elon Musk 和上千名科技领袖联署的公开信呼吁暂停大规模 AI 训练——信中的语气比以往任何类似呼吁都更紧迫。但没有人暂停。5 月，Google 在 I/O 大会上发布 PaLM 2 并将 Bard 升级向公众开放；7 月，Anthropic 发布 Claude 2。AI 界变得像冷战时期的太空竞赛——只是这次有更多的参赛者。

2024 年是"推理模型"元年。OpenAI 发布了 o1（内部代号"草莓"），这个模型在回答问题前会先"思考"——有时思考几分钟——然后再给出答案。它在数学奥赛和编程竞赛上的表现令人瞠目。同时，开源社区异常活跃：Meta 的 LLaMA 3、Mistral 的 Mixtral、阿里的 Qwen 2.5、深度求索的 DeepSeek-V3——大模型不再是少数科技巨头的特权。

2025 年，多模态成为标配。几乎所有主流模型都能处理图文，部分模型（如 Gemini）还能处理音频和视频。1 月，DeepSeek 发布了 R1——一个用强化学习训练出的推理模型，它不需要人类示范"怎么推理"，而是自己探索出了推理策略，其性能可与 OpenAI o1 媲美。AI Agent（智能体）成为新热词——让 AI 不只是回答问题，而是自主执行复杂任务：搜索网页、编写代码、操作软件、甚至管理项目。AI 正从"聊天机器人"进化为"数字工作者"。

有一个细节值得思考：2024 年的诺贝尔物理学奖颁给了 John Hopfield 和 Geoffrey Hinton——我们在第 3 章和第 6 章见过这位 Hinton。诺贝尔物理学奖颁给了一个做神经网络的人。这在十年前是不可想象的。Hinton 本人在获奖后的采访中说："我从未想过这一天会来。我只是一个试图理解大脑如何工作的人。"从感知机到诺贝尔奖——67 年。

→ 当 AI 能推理、能看、能听、能自主行动，我们面对的不再是"如何让它更聪明"的问题——而是"如何确保一个比我们更聪明的系统仍然按照我们的意愿行事"。从第 12 章的对齐问题到今天的推理与多模态，技术的进步一直在加速，但那个最古老的问题——"我们到底想要什么样的 AI？"——始终没有答案。
