# 第 3 章 · 让多层网络学会犯错——反向传播

> "The procedure is so elegant that I have no doubt it will be a major factor in whatever success the field has in the future." — Richard Feynman（据传评价反向传播；真实性存疑，但这句话确实捕捉了那个时代研究者的兴奋）

---

## David Rumelhart、Geoffrey Hinton 和 Ronald Williams 是谁？

1986 年，AI 领域还在第一次寒冬的尾巴上挣扎。经费稀缺，论文难发，大多数计算机科学家对"神经网络"这三个字避之不及。就在这个当口，三个来自不同背景的人凑到了一起，用一篇论文点燃了一把火烧了三十年还没灭的火。

**David Rumelhart** 是三人中最不像"计算机科学家"的一位——因为他根本就不是。1942 年出生的 Rumelhart 是一位认知心理学家，当时在加州大学圣地亚哥分校（UCSD）工作。他的学术兴趣非常纯粹：人类大脑是怎么处理信息的？他不是那种追着热点跑的人，而是一个安静、执着的思考者。他相信，理解心智的最好方法不是写哲学论文，而是构建可以精确模拟心智行为的数学模型。

Rumelhart 当时正在酝酿一个宏大的计划：和同事 James McClelland 一起编辑一套两卷本的巨著《Parallel Distributed Processing》（并行分布式处理，简称 PDP）。这套书 1986 年出版后成为了认知科学的里程碑——它把"联结主义"（connectionism）的思想系统化了，主张智能不是由符号和规则产生的，而是由大量简单单元的相互连接涌现出来的。反向传播的论文，正是这套书的核心技术支撑。遗憾的是，Rumelhart 后来患上了皮克氏病（Pick's disease），一种罕见的早老性痴呆症，于 2011 年去世。他的很多同事都说，如果他能健康地活到 2010 年代深度学习大爆发的时候，他一定会是图灵奖的共同获得者。

**Geoffrey Hinton** 则是这个故事中真正的主角——如果你觉得这章就应该叫"Hinton 的复仇"，你并不孤单。1947 年出生在英国的 Hinton，家世惊人：他的曾曾祖父是数学家 George Boole——没错，就是布尔代数（Boolean algebra）里那个 Boole。他的家族里到处是数学家和逻辑学家。但 Hinton 本人走上这条路，更多是出于一种执拗的个人信念。

Hinton 在剑桥大学读本科的时候学的是实验心理学——和 AI 没什么关系。但他在读书期间被一个想法迷住了：大脑是通过调整神经元之间的连接强度来学习的。这个想法在当时毫无主流地位。1970 年代的英国，AI 的主流是符号主义——用逻辑规则和符号推理来构建智能。Hinton 觉得这条路走不通。他的博士导师 Christopher Longuet-Higgins（爱丁堡大学）也对神经网络有同情态度，但整个学术环境对他们的研究充满质疑。

1978 年拿到博士学位后，Hinton 去了美国。他在加州大学圣地亚哥分校做博士后——对，就是 Rumelhart 所在的学校。两人在那里建立了联系。后来 Hinton 转到卡内基梅隆大学（CMU），然后 1987 年又迁到了加拿大多伦多大学， partly because he didn't want his research funded by the U.S. military（他不想让军方资助自己的研究）。在多伦多，他建立了一个小型神经网络实验室，在接下来近三十年里几乎是全世界唯一持续做深度神经网络研究的地方。

Hinton 的性格和 Minsky 恰好相反：温和、耐心、对任何来请教的学生都愿意花时间聊。但他有一种传教士般的执着——他坚信神经网络是正确方向，即使在全世界都不信的时候。有人问他为什么能在 AI 寒冬中坚持这么久，他说了一句很有名的话："我总觉得只要再多一点时间，就能让它工作起来。"

**Ronald Williams** 是三人中最低调的。他在东北大学（Northeastern University）工作，是一个技术功底深厚的工程师型学者。如果说 Rumelhart 提供了认知科学的直觉、Hinton 提供了架构设计的天赋，那 Williams 就提供了严谨的数学实现和实验验证。在学术史上，很多时候做出关键技术贡献的人并不出名，Williams 就是这样的人。

值得一提的是，反向传播的发明权其实有一段争议。**Paul Werbos** 在 1974 年的哈佛大学博士论文中已经推导出了反向传播算法——比 1986 年早了整整十二年。但 Werbos 的论文淹没在了哈佛的图书馆里，几乎没人引用。1985 年，MIT 的 **David Parker** 也独立发现了同样的算法，同样没有引起什么反响。为什么 Rumelhart、Hinton 和 Williams 的 1986 年论文成功了，而前两位没有？因为 Rumelhart 等人做了一件关键的事：他们不仅给出了算法，还把它放在了《Nature》上——全球最顶级的科学期刊——而且配上了让人信服的实验结果。一个好的想法，如果没有人听到，和不存在没有区别。

## 上一章的遗产：一个没解决的问题

上一章结尾，Minsky 和 Papert 留下了一个令人绝望的问题：两层网络理论上能解决 XOR，但**怎么训练**它？

具体来说，困难在这里：假设你搭了一个 2-2-1 的小网络（两个输入、两个隐藏神经元、一个输出），你给它输入 $(1, 1)$，期待它输出 $0$，但它输出了 $0.5$。你知道错了 $0.5$ 这么多。现在你要调整权重来减小这个误差——但网络里一共有 9 个参数（6 个权重 + 3 个偏置），你该调哪个？调多少？

如果你只调输出层的参数，那就等于只改了最后一道工序——前一道工序送来的"原材料"可能就是错的。但如果你要调隐藏层的参数，你面临一个根本性的困难：隐藏层的输出不是一个直接的"对或错"的判断，它只是一个中间结果。你没有一个明确的"目标值"告诉隐藏神经元"你应该输出 0.7"。它应该输出什么，取决于输出层需要什么——而输出层需要什么，又取决于它自己的参数是什么。

这就像一个公司：最终产品出了问题，CEO 知道结果不对，但中间隔着两道管理层，你不知道该怪市场部还是技术部，该怪经理还是员工。每一层的"正确行为"都依赖于下一层怎么理解它的输出——环环相扣，互相依赖。

这个"信用分配问题"（credit assignment problem）困扰了领域近二十年。直到有人翻开微积分课本，重新看了一眼链式法则。

## 核心洞察：一句话说清楚

**如果正向传播是把信息从输入一层一层推到输出，那反向传播就是把误差从输出一层一层推回输入——而"推"的数学工具就是链式法则。**

想象你在批改一道复杂的多步数学题。学生第一步算了个数 $a$，第二步用 $a$ 算出了 $b$，第三步用 $b$ 算出最终答案 $c$。最终答案 $c$ 和正确答案差了 $\Delta$。你要判断：$a$ 的误差对最终 $\Delta$ 贡献了多少？根据链式法则：$a$ 对 $\Delta$ 的贡献 = $a$ 对 $b$ 的影响 × $b$ 对 $c$ 的影响 × $c$ 对 $\Delta$ 的影响。一层一层往回乘——这就是反向传播在做的事情。

## 数学原理

### 直觉解释

让我们把多层网络想象成一条流水线。原材料（输入数据）从一端进入，经过第一道工序（隐藏层 1），变成半成品；半成品经过第二道工序（隐藏层 2），变成更精细的半成品；……最终经过最后一道工序（输出层），变成成品（预测结果）。

成品和目标之间的差距就是"损失"（loss）。梯度下降告诉我们：要减小损失，每个参数都应该朝"使损失减小最快"的方向挪一小步。但怎么知道哪个方向是"使损失减小最快"的？你需要计算损失对每个参数的梯度——偏导数。

对一个在输出层的参数来说，这很简单：它直接影响输出，输出直接影响损失，一步求导就搞定。但对一个在隐藏层的参数呢？它先影响隐藏层的输出，隐藏层的输出影响下一层的输入，下一层的输入影响下一层的输出……一路传下去，直到影响最终损失。一条长长的因果链。

链式法则（chain rule）正是用来处理这种"长链依赖"的数学工具。它的精髓是：**如果你知道链条中每一环对下一环的影响（局部导数），你就能算出第一环对最后一环的影响（全局梯度）。** 而且你不需要重新分析整条链——你只需要从最后一环开始，把局部导数一个一个乘回去。

这就是"反向传播"名字的由来：信号正向走一遍（算出输出），误差反向走一遍（算出梯度）。前向是推算结果，后向是追究责任。

### 关键公式

**1. 均方误差损失函数：**

$$L = \frac{1}{2}(o - y)^2$$

其中 $o$ 是网络的输出，$y$ 是目标值。系数 $\frac{1}{2}$ 纯粹为了求导后好看——消掉平方带来的 2。

**2. 输出层的误差信号：**

$$\delta_{\text{out}} = (o - y) \cdot \sigma'(z_{\text{out}})$$

其中 $\sigma'(z) = \sigma(z)(1 - \sigma(z))$ 是 sigmoid 函数的导数，$z_{\text{out}}$ 是输出层神经元的加权和。这个 $\delta$ 就是"误差信号"——它衡量了输出层对最终损失负有多少"即时责任"。

**3. 误差信号的反向传播（核心公式）：**

$$\delta_j = \left(\sum_k \delta_k \cdot v_{jk}\right) \cdot \sigma'(z_j)$$

这是全章最关键的公式。它说的是：隐藏层神经元 $j$ 的误差信号 $\delta_j$，等于它下游（第 $k$ 层）所有误差信号的加权和 $\sum_k \delta_k \cdot v_{jk}$，再乘以它自身的激活函数导数 $\sigma'(z_j)$。注意 $v_{jk}$ 是从神经元 $j$ 连向下游神经元 $k$ 的权重——**误差信号沿着和前向传播相同的连接线路反向流动，只是方向相反而已。**

直觉含义：神经元 $j$ 对最终误差的"责任"，取决于（1）它对接下来的层输出了多少信号（通过 $v_{jk}$ 衡量），以及（2）它的输出对输入变化有多敏感（通过 $\sigma'$ 衡量）。这正好是链式法则的精神——局部贡献的乘积给出全局梯度。

### 手算例子

让我们亲手用反向传播训练一个 2-2-1 网络来解决上一章遗留的 XOR 问题。

**网络结构：**

- 输入层：$x_1, x_2$（两个输入）
- 隐藏层：$h_1, h_2$（两个神经元，sigmoid 激活）
- 输出层：$o$（一个神经元，sigmoid 激活）

**初始权重（精心选取的初始值）：**

| 连接 | 权重 |
|:---|:---:|
| $x_1 \to h_1$ | $w_{11} = 1$ |
| $x_2 \to h_1$ | $w_{21} = -0.5$ |
| $h_1$ 偏置 | $b_1 = -0.5$ |
| $x_1 \to h_2$ | $w_{12} = -1$ |
| $x_2 \to h_2$ | $w_{22} = 0.5$ |
| $h_2$ 偏置 | $b_2 = 0.5$ |
| $h_1 \to o$ | $v_1 = 1$ |
| $h_2 \to o$ | $v_2 = -1$ |
| $o$ 偏置 | $b_3 = 0$ |

**第一步：前向传播（输入 $x = (1,1)$，目标 $y = 0$）**

隐藏层：

$$z_1 = w_{11} x_1 + w_{21} x_2 + b_1 = 1 \times 1 + (-0.5) \times 1 + (-0.5) = 0$$

$$h_1 = \sigma(0) = 0.5$$

$$z_2 = w_{12} x_1 + w_{22} x_2 + b_2 = (-1) \times 1 + 0.5 \times 1 + 0.5 = 0$$

$$h_2 = \sigma(0) = 0.5$$

输出层：

$$z_3 = v_1 h_1 + v_2 h_2 + b_3 = 1 \times 0.5 + (-1) \times 0.5 + 0 = 0$$

$$o = \sigma(0) = 0.5$$

损失：

$$L = \frac{1}{2}(o - y)^2 = \frac{1}{2}(0.5 - 0)^2 = 0.125$$

网络的输出是 $0.5$，而目标是 $0$。没学到任何东西——这是因为我们精心选取的初始权重恰好让所有中间结果都落在了 $0$ 上，sigmoid 在 $0$ 处输出 $0.5$。现在让我们看看反向传播能否改变这个局面。

**第二步：反向传播——计算梯度**

输出层误差信号（$\sigma'(0) = 0.5 \times 0.5 = 0.25$）：

$$\delta_3 = (o - y) \cdot \sigma'(z_3) = 0.5 \times 0.25 = 0.125$$

输出层权重梯度：

$$\frac{\partial L}{\partial v_1} = \delta_3 \times h_1 = 0.125 \times 0.5 = 0.0625$$

$$\frac{\partial L}{\partial v_2} = \delta_3 \times h_2 = 0.125 \times 0.5 = 0.0625$$

$$\frac{\partial L}{\partial b_3} = \delta_3 = 0.125$$

隐藏层误差信号——**这是反向传播最关键的一步**：

$$\delta_1 = (\delta_3 \times v_1) \cdot \sigma'(z_1) = (0.125 \times 1) \times 0.25 = 0.03125$$

$$\delta_2 = (\delta_3 \times v_2) \cdot \sigma'(z_2) = (0.125 \times (-1)) \times 0.25 = -0.03125$$

注意发生了什么：$\delta_3$ 沿着权重 $v_1 = 1$ 和 $v_2 = -1$ 流回了隐藏层。因为 $v_2$ 是负的，所以 $\delta_2$ 的符号和 $\delta_1$ 相反——隐藏神经元 $h_2$ 收到的"责任信号"是负的，意味着它应该**增加**自己的输出来帮助减小误差。这正是"沿着连接反向流动"的含义。

隐藏层权重梯度：

$$\frac{\partial L}{\partial w_{11}} = \delta_1 \times x_1 = 0.03125 \times 1 = 0.03125$$

$$\frac{\partial L}{\partial w_{21}} = \delta_1 \times x_2 = 0.03125 \times 1 = 0.03125$$

$$\frac{\partial L}{\partial b_1} = \delta_1 = 0.03125$$

$$\frac{\partial L}{\partial w_{12}} = \delta_2 \times x_1 = -0.03125$$

$$\frac{\partial L}{\partial w_{22}} = \delta_2 \times x_2 = -0.03125$$

$$\frac{\partial L}{\partial b_2} = \delta_2 = -0.03125$$

**第三步：更新权重（学习率 $\eta = 1$）**

每个参数减去梯度（梯度下降：朝让损失减小的方向走一步）：

$$v_1 \leftarrow 1 - 0.0625 = 0.9375, \quad v_2 \leftarrow -1 - 0.0625 = -1.0625, \quad b_3 \leftarrow 0 - 0.125 = -0.125$$

$$w_{11} \leftarrow 1 - 0.03125 = 0.96875, \quad w_{21} \leftarrow -0.5 - 0.03125 = -0.53125, \quad b_1 \leftarrow -0.5 - 0.03125 = -0.53125$$

$$w_{12} \leftarrow -1 - (-0.03125) = -0.96875, \quad w_{22} \leftarrow 0.5 - (-0.03125) = 0.53125, \quad b_2 \leftarrow 0.5 - (-0.03125) = 0.53125$$

**第四步：验证——再用同一个输入前向传播一次**

$$z_1 = 0.96875 - 0.53125 - 0.53125 = -0.09375 \implies h_1 = \sigma(-0.09375) \approx 0.477$$

$$z_2 = -0.96875 + 0.53125 + 0.53125 = 0.09375 \implies h_2 = \sigma(0.09375) \approx 0.523$$

$$z_3 = 0.9375 \times 0.477 + (-1.0625) \times 0.523 + (-0.125) \approx -0.234$$

$$o = \sigma(-0.234) \approx 0.442$$

输出从 $0.500$ 降到了 $0.442$——**向目标 $0$ 迈进了一步**。经过几百次这样的迭代（每次对四个 XOR 输入都做一遍），网络最终会收敛到正确的 XOR 函数。

这就是反向传播的魔力：每一次迭代，每个参数都精确地知道自己该往哪个方向动、动多少。近二十年的未解难题，被一条微积分课本上的定理解开了。

## 这解决了一切吗？——新的局限

反向传播解决了一个存在论层面的问题——"多层网络能不能训练"的回答从"不知道"变成了"能"。但这并不意味着万事大吉。

第一个问题是**速度**。反向传播在 1980 年代的小网络上工作得很好——十几个神经元、几十个参数。但如果你想让网络更深——五层、十层、二十层——训练时间会急剧增长。每多一层，误差信号就要多传一次，而每次传递都涉及矩阵乘法和链式法则的递归应用。在 1986 年的硬件上，训练一个稍微大一点的网络可能需要好几天。

第二个问题是**局部最优**。梯度下降保证你能找到损失函数的"下坡方向"，但它不保证你能找到最低的谷底。损失函数的"地形"（loss landscape）通常充满了小山丘和小凹坑——你很容易滑进一个"局部最低点"然后卡住，以为自己到了谷底，其实只是到了路边的某个水坑。更深的网络，这个问题越严重。

第三个问题更根本：**缺乏理论保证**。感知机有收敛定理——只要数据线性可分，它一定能学会。SVM 后来会有泛化误差界——你能从理论上预测它在未见数据上的表现。但反向传播训练的多层网络？你不知道它为什么工作、什么时候会失败、训练出来的模型到底有多可靠。对很多严谨的研究者来说，这感觉更像是"炼金术"而不是科学。

这些问题在 1990 年代变得如此刺眼，以至于整个领域又一次转向了另一个方向——一个有严格理论保证的方向。那个方向的主角是一个苏联数学家，他的武器不是链式法则，而是凸优化。

## 这一年的世界

1986 年的世界正处在一个巨大的转折点上。1 月 28 日，挑战者号航天飞机在升空 73 秒后爆炸，七名宇航员全部遇难。一个由密封圈失效引发的技术灾难，对"工程技术可以征服一切"的信心造成了沉重打击。4 月 26 日，切尔诺贝利核电站发生人类历史上最严重的核事故。这一年，世界被迫学会了一课：复杂系统的失败代价可能是灾难性的。

AI 领域本身也在经历微妙的变化。虽然"第一次 AI 寒冬"还没完全结束——主流的 AI 经费仍然流向专家系统和知识工程——但 Rumelhart 和 McClelland 的 PDP 两卷本在认知科学圈子里引起了轰动。心理学家和神经科学家们突然发现：神经网络不仅仅是计算机科学的玩具，它可能是理解人类认知的数学语言。这个跨学科的视角为后来神经网络的复兴埋下了种子。

与此同时，在卡内基梅隆大学，一辆叫 Navlab 的自动驾驶面包车正在校园里慢慢蠕动——它使用了神经网络来处理视觉信号。在波士顿，一家叫 Thinking Machines 的公司造出了 Connection Machine——一台拥有 65,536 个处理器的超级计算机，专门为并行计算设计。它的名字暗示了一种信念：智能，也许就是大规模并行处理的涌现属性。

Hinton 本人在 1987 年搬到了多伦多。他后来回忆说，加拿大对他来说是一个"能安安静静做自己想做的事"的地方。在那里，他建立了一个几乎可以称之为"神经网络修道院"的研究小组。在接下来的二十多年里，当全世界都在追逐 SVM、核方法、贝叶斯推断的时候，这个修道院里的僧侣们一直在做同一件事：让神经网络更深、更强。

→ 多层网络终于能训练了，但训练慢、容易卡住、没有理论保证——有没有一种方法既有神经网络的灵活性，又有数学定理的可靠性？
