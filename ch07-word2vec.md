# 第 7 章 · 把词语放进空间——词嵌入与 Word2Vec

> "You shall know a word by the company it keeps." — J.R. Firth，1957

---

## Tomas Mikolov 是谁？

2012 年的 Google Brain 团队，聚集了当时世界上最聪明的一群深度学习研究者。但在这群人中间，有一个不太合群的捷克人——他不爱参加学术会议的社交活动，不善于在论文里讲漂亮的故事，甚至在 Google 内部也不是最受关注的那几位。但他有一种罕见的直觉：他知道怎样让复杂的东西变得极其简单。

**Tomas Mikolov** 1982 年出生在捷克的 Šumperk（舒姆佩尔克），在中欧的技术传统中长大。他在布尔诺理工大学（Brno University of Technology）完成了硕士和博士学业。Mikolov 不是那种从小就被视为"天才"的人——他的学术起点并不显赫，博士论文在布尔诺理工大学完成，导师 Jan Černocký 也不是深度学习的圈内名流。

但 Mikolov 有一项被低估的天赋：**他痴迷于语言模型**。不是那种"理论上优雅"的痴迷，而是一种工程师式的执念——他想让机器真正理解语言的统计规律。早在 2007 年，他就开始研究循环神经网络（RNN）在语言模型中的应用，比大多数人都早。他的 RNN 语言模型论文在 2010 年的 INTERSPEECH（国际语音通信年会）上发表，引起了广泛关注——那时候，用神经网络做语言模型还是一个很小众的方向。

2012 年，Mikolov 被 Google Brain 团队招致麾下。这个团队刚刚见证了 AlexNet 的惊人成功（第 6 章的故事），整个团队都在思考一个问题：深度学习的下一站在哪里？图像已经被拿下了，那语言呢？

Mikolov 带着自己的答案来到了 Google。他发现，之前所有用神经网络做语言模型的人——包括 Bengio 2003 年的开创性工作——都犯了一个共同的"错误"：他们把问题搞得太复杂了。Bengio 的神经语言模型用了一个多层前馈网络来预测下一个词，效果不错但训练极慢，根本无法扩展到大规模数据。

Mikolov 做了一件在学术圈不太常见的事：他没有试图改进 Bengio 的架构，而是几乎把它拆成了最简单的碎片。他把"预测下一个词"这个大任务拆成了无数个极小的子任务——每一个只需要看一个中心词，预测它的邻居。不需要多层网络，不需要复杂的非线性变换，只需要一个两层的"浅"网络。

这个想法听起来太简单了，简单到很多人觉得不值一提。但 Mikolov 发现，正是这种简化带来了惊人的效率——训练速度提升了几十倍，使得在数十亿词的语料上训练成为可能。而一旦数据量足够大，这个简单模型学到的词向量展现出了令人震惊的语义性质：向量之间竟然可以做加减法，而且结果有意义。

2013 年，Mikolov 的团队连续发表了两篇论文，分别介绍了两种架构——**CBOW**（Continuous Bag-of-Words）和 **Skip-gram**。这两篇论文没有复杂的数学推导，没有精心设计的实验对比，甚至没有太多引用前人工作的篇幅。但它们改变了整个 NLP 的方向。

Mikolov 后来从 Google 离开，先后去了 Facebook AI Research（FAIR）和捷克 CIIRC（捷克信息学、机器人与控制论研究所）。他始终保持着一种独立的、不太"主流"的研究风格——不追逐热点，不写大模型论文，而是持续探索语言理解的本质。这让他显得有些"边缘"，但了解他工作的人都知道：正是这种边缘人，做出了 NLP 领域最具革命性的贡献之一。

## 上一章的遗产：一个没解决的问题

第 6 章的 AlexNet 用 GPU、ReLU 和卷积征服了图像识别。但这场胜利留下了一个巨大的空白：自然语言处理（NLP）仍然被挡在深度学习的门外。

为什么？因为深度学习的一切都建立在"向量运算"的基础上——你的数据必须是向量（一组有序的数字），才能喂给神经网络。图像天然就是向量：一张 $28 \times 28$ 的灰度图像就是 784 个像素值，每个像素是一个 0 到 255 之间的数字。神经网络在这些数字上做矩阵乘法、求导、更新参数——一切顺理成章。

但文字不是向量。"猫"是一个概念，不是一个数字。

当时的主流方法是 **one-hot 编码**（one-hot encoding）。具体怎么做？假设你的词汇表有 10 万个词，按字母顺序编号。那么"猫"是第 3847 个词，它就表示为一个 10 万维的向量：第 3847 个位置是 1，其余 99999 个位置全是 0。

$$\text{猫} = (0, 0, \ldots, 0, \underbrace{1}_{\text{第3847位}}, 0, \ldots, 0)$$

$$\text{狗} = (0, 0, \ldots, 0, \underbrace{1}_{\text{第5621位}}, 0, \ldots, 0)$$

这种表示有一个致命的问题。让我们算一下"猫"和"狗"之间的余弦相似度（一种衡量向量相似程度的方法）：

$$\cos(\text{猫}, \text{狗}) = \frac{\text{猫} \cdot \text{狗}}{|\text{猫}||\text{狗}|} = \frac{0}{1 \times 1} = 0$$

点积为零——因为"猫"的向量中唯一非零的位置和"狗"的唯一非零位置不在同一个地方，乘起来全是零。那"猫"和"桌子"呢？也是 0。"猫"和"量子力学"呢？还是 0。**所有词之间的距离完全相等。**

这太荒谬了。在人类的概念空间里，"猫"和"狗"显然比"猫"和"桌子"更相近——它们都是动物、都有毛、都是宠物。但 one-hot 编码完全丢失了这些语义信息。用 one-hot 喂给神经网络，就像把一张图像的所有像素随机打乱顺序——神经网络看到的只是一堆毫无结构的数字。

深度学习需要一种把词变成有意义向量的方法——一种让"意义相近的词在空间中相近"的方法。这个问题不解决，AlexNet 的成功就无法复制到语言领域。

## 核心洞察：一句话说清楚

**一个词的含义，由它周围出现的词决定——因此，我们可以通过统计"哪些词经常和它一起出现"来学习这个词的向量表示。**

想象你是一个语言学家，在研究一门完全陌生的语言。你拿到了一本用这种语言写的书，但没有字典。你怎么弄懂每个词的意思？最有效的方法是看"上下文"：如果一个词经常出现在"太阳"、"温暖"、"白天"附近，它很可能是"明亮"或者"晴朗"；如果它经常出现在"筷子"、"菜单"、"厨师"附近，那大概是某种食物。英国语言学家 J.R. Firth 在 1957 年说："You shall know a word by the company it keeps"——你通过一个词的伙伴来认识这个词。Mikolov 做的事情，就是把这句语言学格言变成了一个可计算的数学过程。

## 数学原理

### 直觉解释

Word2Vec 的核心思想异常简洁。它要做的事情只有一件：**训练一个神经网络，让它能从上下文预测中心词（或者反过来，从中心词预测上下文）**。在训练过程中，网络被迫为每个词学到一个向量——这个向量必须包含足够的语义信息，才能做好预测任务。

两种架构分别对应两种预测方向：

**CBOW**（Continuous Bag-of-Words）：给你一个词周围的几个词（上下文），让你猜中间那个词是什么。好比考试里做"完形填空"——"今天天气很___，我们去公园散步"中的空格，你猜是"好"。

**Skip-gram**：反过来，给你一个词，让你猜它周围可能出现哪些词。好比"我看到'猫'这个词，猜猜它附近可能出现什么？——'狗'、'鱼'、'可爱'？"

两种方法效果差不多，但 Skip-gram 在实践中更常用，因为它对罕见词的效果更好。我们重点看 Skip-gram。

训练的目标是：让网络给"真正出现在中心词旁边"的上下文词打高分，给"不太可能出现"的词打低分。训练完成后，网络中存储的每个词的向量（叫**词嵌入**，word embedding）就自然地编码了语义信息——因为语义相近的词出现在相似的上下文中，它们的向量自然也相近。

整个过程完全不需要人工标注——你只需要大量的文本（维基百科、新闻、网页……），网络就能自己学会哪些词是"朋友"（经常一起出现），哪些词是"陌生人"（几乎从不一起出现）。

### 关键公式

**1. Skip-gram 的目标函数——让邻居词的概率最大：**

$$J = \frac{1}{T} \sum_{t=1}^{T} \sum_{-c \leq j \leq c, j \neq 0} \log P(w_{t+j} \mid w_t)$$

其中 $T$ 是语料库的总词数，$c$ 是上下文窗口大小（中心词前后各看 $c$ 个词），$P(w_{t+j} \mid w_t)$ 是给定中心词 $w_t$ 后，上下文词 $w_{t+j}$ 出现的概率。直觉：对语料库中每一对 (中心词, 上下文词)，我们都想让"上下文词在中心词旁边出现"的概率尽可能大。最大化这个函数，就是让模型最"确信"哪些词应该在一起。

**2. 概率用 softmax 计算：**

$$P(w_O \mid w_I) = \frac{\exp(\mathbf{v}_{w_O}^{\top} \mathbf{u}_{w_I})}{\sum_{w=1}^{V} \exp(\mathbf{v}_{w}^{\top} \mathbf{u}_{w_I})}$$

其中 $\mathbf{u}_{w_I}$ 是中心词 $w_I$ 的向量（$d$ 维），$\mathbf{v}_{w_O}$ 是上下文词 $w_O$ 的向量（也是 $d$ 维），$V$ 是词汇表大小。直觉：两个词的向量越相似（点积越大），它们一起出现的概率就越高。分母是所有词的点积之和——这保证了概率是一个合法的分布（所有概率之和为 1）。

**3. 负采样——让训练变快的关键技巧：**

$$\log \sigma(\mathbf{v}_{w_O}^{\top} \mathbf{u}_{w_I}) + \sum_{k=1}^{K} \mathbb{E}_{w_k \sim P_n(w)}[\log \sigma(-\mathbf{v}_{w_k}^{\top} \mathbf{u}_{w_I})]$$

其中 $\sigma(x) = 1/(1+e^{-x})$ 是 sigmoid 函数，$K$ 个"负样本" $w_k$ 从噪声分布 $P_n$ 中采样。直觉：与其计算所有 $V$ 个词的概率（$V$ 可能是 10 万，计算量巨大），不如只看一个正样本（真正出现的上下文词）和 $K$ 个随机采样的负样本（不太可能出现的词）。让正样本得分高，负样本得分低，就够了。这让训练速度提升了几个数量级。

### 手算例子

让我们用一个极小的"语料库"手动走一遍 Skip-gram 的一次梯度更新。

**语料库**（只有一句话）：

> "猫 喜欢 吃 鱼 而且 狗 也 喜欢 吃 肉"

为了简化，假设词汇表只有这 9 个词（编号 1-9）：猫、喜欢、吃、鱼、而且、狗、也、喜欢（重复）、肉。去重后 8 个词。

**初始化**：给每个词随机初始化一个 2 维的中心词向量 $\mathbf{u}$ 和上下文向量 $\mathbf{v}$：

| 词 | $\mathbf{u}$（中心词向量） | $\mathbf{v}$（上下文向量） |
|---|---|---|
| 猫 | $(0.1, 0.2)$ | $(0.3, -0.1)$ |
| 喜欢 | $(-0.1, 0.3)$ | $(0.2, 0.1)$ |
| 吃 | $(0.2, -0.1)$ | $(-0.2, 0.3)$ |
| 鱼 | $(0.3, 0.1)$ | $(0.1, 0.2)$ |

**训练对**：取 (中心词 = "猫", 上下文词 = "喜欢")，窗口大小 $c = 1$。

**Step 1：计算点积。**

$$s = \mathbf{v}_{\text{喜欢}}^{\top} \mathbf{u}_{\text{猫}} = 0.2 \times 0.1 + 0.1 \times 0.2 = 0.02 + 0.02 = 0.04$$

**Step 2：对于正样本，sigmoid 输出。**

$$\sigma(0.04) = \frac{1}{1 + e^{-0.04}} \approx \frac{1}{1 + 0.961} \approx 0.51$$

这个值接近 0.5，意味着模型"不太确定""喜欢"会不会出现在"猫"旁边——这正是我们期望的，因为向量还是随机的。

**Step 3：取一个负样本**，假设随机抽到"鱼"。计算点积：

$$s_{\text{neg}} = \mathbf{v}_{\text{鱼}}^{\top} \mathbf{u}_{\text{猫}} = 0.1 \times 0.1 + 0.2 \times 0.2 = 0.01 + 0.04 = 0.05$$

$$\sigma(-0.05) \approx 0.488$$

**Step 4：计算梯度并更新。** 学习率 $\eta = 0.1$。

对于正样本"喜欢"的上下文向量：

$$\mathbf{v}_{\text{喜欢}} \leftarrow \mathbf{v}_{\text{喜欢}} + \eta \cdot (1 - 0.51) \cdot \mathbf{u}_{\text{猫}}$$
$$= (0.2, 0.1) + 0.1 \times 0.49 \times (0.1, 0.2)$$
$$= (0.2, 0.1) + (0.0049, 0.0098) = (0.2049, 0.1098)$$

对于负样本"鱼"的上下文向量（我们要**减小**它的得分）：

$$\mathbf{v}_{\text{鱼}} \leftarrow \mathbf{v}_{\text{鱼}} - \eta \cdot (1 - 0.488) \cdot \mathbf{u}_{\text{猫}}$$
$$= (0.1, 0.2) - 0.1 \times 0.512 \times (0.1, 0.2)$$
$$= (0.1, 0.2) - (0.0051, 0.0102) = (0.0949, 0.1898)$$

**发生了什么？** 经过这一步更新：
- "喜欢"的上下文向量 $\mathbf{v}$ 朝"猫"的中心词向量 $\mathbf{u}$ 的方向**靠近了一点点**——因为它们是真正的邻居，我们希望它们的点积变大。
- "鱼"的上下文向量 $\mathbf{v}$ 朝反方向**远离了一点点**——因为在这次训练中，"鱼"被当作了负样本。

当成千上万个训练对都被这样处理后，"猫"和"狗"会因为出现在相似的上下文中（"喜欢""吃"……），它们的向量逐渐靠近；而"猫"和"桌子"几乎从不共现，它们的向量会远离。最终，向量空间中出现一种美丽的结构：

$$\mathbf{u}_{\text{国王}} - \mathbf{u}_{\text{男人}} + \mathbf{u}_{\text{女人}} \approx \mathbf{u}_{\text{女王}}$$

向量之间可以进行有意义的加减法。"国王"减去"男人"加上"女人"约等于"女王"——这意味着向量空间中的某个方向编码了"性别"这个语义维度，而另一个方向编码了"皇室"这个维度。这不是被人为设计出来的，而是模型从大量文本中**自动发现**的。

## 这解决了一切吗？——新的局限

Word2Vec 是 NLP 领域的一次范式转换。在此之前，"词"和"数学"是两个几乎不相交的世界——你不可能对"猫"做加减乘除。在此之后，词变成了向量空间中的点，语义关系变成了几何距离和方向。整个 NLP 的工具箱——神经网络、梯度下降、矩阵分解——突然全都可用了。

但 Word2Vec 有一个根本性的局限：**每个词只有一个向量**。

"苹果"在"我吃了一个苹果"和"苹果公司发布了新手机"中，含义完全不同——前者是水果，后者是科技公司。但 Word2Vec 给"苹果"分配的唯一一个向量，只能表示这两种含义的平均——一个既不是水果也不是公司的模糊向量。

更深层的问题是：Word2Vec 只在**词**的层面上工作。它知道"猫"和"狗"相似，但它不理解句子。一个句子的含义不是把每个词的向量加起来就能得到的——"狗咬人"和"人咬狗"包含相同的词，但意思完全不同。Word2Vec 没有办法处理词序和句子结构。

这些局限在简单的任务（情感分析、文档分类）上不太明显，但在需要理解整个句子的任务上——比如机器翻译——就变成了致命的瓶颈。翻译不是逐词替换，你需要理解整个源句子，然后生成另一个语言的目标句子。为此，你需要一种能处理**整个序列**的模型架构。

与此同时，一个更实际的问题也在浮现：如果要用深度学习做翻译，你怎么把一个变长的源句子"喂"给一个神经网络？传统的分类网络需要固定长度的输入，但句子的长度千变万化。你需要一种新的架构，把一个变长序列编码成一个有意义的内部表示，再把它解码成另一个变长序列。

## 这一年的世界

2013 年的 AI 领域处于一种奇特的"黎明前的黎明"状态。AlexNet 已经在 2012 年底证明了深度学习的威力，但这场革命的涟漪还没有扩散到普通公众的意识中。当时最火的科技话题是可穿戴设备——Google Glass 刚刚发布，人们争论"眼镜上装摄像头是不是侵犯隐私"。

在学术界，深度学习仍然是一个"有人激动、有人怀疑"的领域。NLP 圈子尤其保守——主流方法是精心设计的统计模型（如 CRF、HMM），加上大量手工设计的语言学特征。当 Mikolov 的 Word2Vec 论文在 2013 年初出现时，很多 NLP 研究者的第一反应不是"太棒了"，而是"这不就是一个简化的神经网络吗？凭什么这么简单的模型效果这么好？"

Mikolov 的回应方式很"工程师"：他没有写长篇论文反驳，而是直接开源了代码。Word2Vec 的 C 语言实现被放在了 Google Code 上，任何人都可以下载、编译、在自己的数据上跑。这种"代码说话"的策略非常有效——当其他研究者用 Word2Vec 在自己的数据上训练出高质量的词向量，并且发现向量运算确实能捕获语义关系时，质疑声很快就消失了。

同一时期，Google 和 Facebook 正在进行激烈的 AI 人才争夺战。Facebook 在 2013 年底成立了 FAIR（Facebook AI Research），由 Yann LeCun（卷积神经网络的先驱之一）担任负责人。Google Brain 则从 Google X 实验室独立出来，成为正式的研究部门。两大科技公司的研究实验室开始像大学一样发表论文、培养博士生——但经费是大学的十倍。

一个有趣的细节：Mikolov 的 Word2Vec 论文最初被顶级会议拒稿了。审稿人认为这个方法"太简单"，不值得在顶会上发表——它收到了四个"强烈拒绝"的评审意见。最终它只被 ICLR 2013 的工作坊（workshop）接收，而非正式会议。但几年后回顾，这篇"太简单"的论文成了 NLP 领域被引用最多的工作之一，直接催生了后续所有的预训练语言模型——包括 BERT 和 GPT。

→ 词向量让 NLP 进入了向量空间的时代，但"每个词只有一个向量"的限制意味着模型无法区分多义词，也无法理解整个句子的含义——要真正处理语言，我们需要一种能够编码整个序列、并且动态关注重要信息的架构。
