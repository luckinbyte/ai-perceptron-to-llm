# 第 11 章 · 先通读再答题——预训练语言模型（BERT 与 GPT）

> "We show that pre-training on a large corpus of unlabeled text is crucial for achieving strong performance." — Devlin et al., 2018

---

## Jacob Devlin 和 Alec Radford 是谁？

2018 年初，两个独立的研究团队几乎同时做出了一个相同的赌注：让 Transformer 先"大量阅读"无标注文本，再去解决具体的 NLP 任务。一个团队在 Google，另一个在 OpenAI。两条技术路线的核心思想惊人地一致，但实现方式截然不同——一个选择了"双向"，另一个选择了"单向"。

**Jacob Devlin** 是 Google AI Language 团队的研究科学家。在 BERT 之前，Devlin 的主要工作是 Google 的神经机器翻译系统——没错，正是第 10 章 Transformer 论文发表后、被 Google Translate 采用的那套系统。Devlin 是一个低调而务实的人，在 AI 研究的"明星文化"中显得有些格格不入。他不热衷于社交媒体上的论战，也不追逐学术界的潮流。但他有一个令人敬畏的特点：对细节的偏执。Devlin 的同事曾半开玩笑地说，他能花一周时间优化一个数据预处理的步骤，然后这个优化让整个系统提升了两个百分点。

在开发 BERT 之前，Devlin 注意到了一个被很多人忽视的事实：2018 年初，两个独立的预训练语言模型——ELMo（来自 AI2 的 Matthew Peters 等人）和 ULMFiT（来自 fast.ai 的 Jeremy Howard 和 Sebastian Ruder 等人）——都已经证明了"先预训练、再微调"的思路在 NLP 中是有效的。但它们用的还是 LSTM，不是 Transformer。Devlin 的洞察很直接：如果把预训练和 Transformer 结合起来，效果会不会有一个量级的飞跃？BERT 就是这个问题的答案。

**Alec Radford** 走的是一条完全不同的路。他是 OpenAI 的研究科学家，也是一个"生成派"的坚定信徒。2017 年的 OpenAI 还是一个纯粹的非营利研究机构（Elon Musk 还在董事会里），规模不大，但有一种"改变世界"的理想主义氛围。Radford 在 OpenAI 的早期工作集中在生成模型上——他相信，一个能"生成"语言的模型，必然已经理解了语言的深层结构。

Radford 的风格和 Devlin 截然不同。如果说 Devlin 是一个精雕细琢的工匠，Radford 更像一个大胆的探险家。GPT-1 的论文标题就很有 OpenAI 的风格——"Improving Language Understanding by Generative Pre-Training"——朴素但野心勃勃。Radford 和他的合作者们（包括 Ilya Sutskever，我们第 9 章 Seq2Seq 的作者之一）选择了一条看似"更笨"的路：不像 BERT 那样让模型做"完形填空"（利用左右两侧的上下文），而是让它做"预测下一个词"——只看左边，不看右边。

这两个人的选择——双向 vs 单向、完形填空 vs 预测下一个词——在 2018 年引发了一场影响深远的辩论。但无论路线如何，他们共同证明了一件事：**先通读再答题，远比拿到题目才开始学习要强得多。**

## 上一章的遗产：一个没解决的问题

第 10 章的 Transformer 架构是一个巨大的飞跃——序列中所有位置可以并行计算，训练速度比 RNN 快了几个数量级，机器翻译的质量也刷新了记录。但 Transformer 有一个让所有 NLP 研究者头疼的问题：**它是为特定任务定制的。**

让我们用具体的例子来感受这个痛点。假设你想做三件 NLP 任务：

1. **情感分析**：判断"这部电影太精彩了"是正面还是负面
2. **问答系统**：给定一段文字和一个问题，找出答案在文中的位置
3. **命名实体识别**：在文本中标出人名、地名、机构名

在 2017 年，你需要为每个任务做三件事：收集标注数据（每个任务几千到几万条）、设计任务特定的网络结构（分类器、序列标注器、span 预测器等）、从头训练一个 Transformer。标注数据的获取成本极高——情感分析需要人工标注，问答系统需要专家编写，命名实体识别更是费时费力。

更令人沮丧的是：三个任务明明都在"理解语言"，你在任务 A 上学到的东西无法迁移到任务 B。这就像一个学生，每次考试都要从头学起——明明已经读了十年书，积累了大量知识，但每次新考试都把他当作白纸一张。

这合理吗？

## 核心洞察：一句话说清楚

**让模型先在海量无标注文本上学会"理解语言"，再用少量标注数据"适应任务"——就像学生先大量阅读打基础，再针对性刷题备考。**

想象一个医学生。在成为一名心脏科医生之前，他先在医学院里学习人体解剖、生理、病理——这些知识不特定于任何科室，但对所有科室都有用。这就是"预训练"（pre-training）。然后他在心脏科做住院医师，专门学习心脏病的诊断和治疗。这就是"微调"（fine-tuning）。没有人会从零开始培养一个只懂心脏、连基本解剖学都不知道的医生。

BERT 和 GPT 做的就是这件事：让 Transformer 先当"医学生"（在大规模文本上预训练），再当"专科医生"（在特定任务上微调）。

## 数学原理

### 直觉解释

BERT 和 GPT 的核心思想是**迁移学习**（transfer learning）：把从源领域学到的知识迁移到目标领域。在 NLP 中，"源领域"就是海量无标注文本——互联网上的维基百科、新闻、书籍、论坛帖子。"目标领域"是具体的下游 NLP 任务。

两者的区别在于预训练的方式——也就是让模型做"什么样的练习题"。

**BERT** 选择了"完形填空"。给定一句话 "The cat sat on the mat"，BERT 随机遮盖（mask）其中 15% 的词，变成 "The [MASK] sat on the [MASK]"，然后让模型根据**左右两侧**的上下文来预测被遮盖的词。这种方式叫**遮盖语言模型**（Masked Language Model，MLM）。因为是双向的——模型同时看到了 "The" 和 "sat on the mat"——所以 BERT 能理解完整的上下文。

**GPT** 选择了"预测下一个词"。给定 "The cat sat on"，GPT 要预测下一个最可能的词是 "the"；然后给定 "The cat sat on the"，预测下一个是 "mat"。这种方式叫**自回归语言模型**（Autoregressive Language Model）。因为是单向的——模型只能看到左边已经出现的词——所以 GPT 天生是一个"生成"模型。

你可以用一个类比来理解两者的区别。BERT 像在做阅读理解：给你一段话，中间挖掉几个词，你需要根据前后文填回去——你必须同时理解文章的前半部分和后半部分。GPT 像在续写故事：给你故事的开头，你一段一段往下写——你只能基于已经写好的部分来构思下一步。两种方式都能让模型学会语言的规律，但侧重点不同。

### 关键公式

**1. BERT 的遮盖语言模型（MLM）损失：**

$$\mathcal{L}_{\text{MLM}} = -\sum_{i \in \text{masked}} \log P(x_i \mid x_1, \ldots, x_{i-1}, x_{i+1}, \ldots, x_n)$$

其中 $x_i$ 是被遮盖的第 $i$ 个 token，$x_1, \ldots, x_{i-1}, x_{i+1}, \ldots, x_n$ 是序列中所有**未被遮盖**的 token（即 $x_i$ 左右两侧的上下文）。直觉：在所有被遮盖的位置上，最大化正确词的对数概率。模型必须同时利用左侧和右侧的信息来推断中间缺失的词——这正是 BERT 比单向模型更"理解"上下文的原因。

**2. GPT 的自回归语言模型损失：**

$$\mathcal{L}_{\text{LM}} = -\sum_{t=1}^{n} \log P(x_t \mid x_1, x_2, \ldots, x_{t-1})$$

其中 $x_t$ 是第 $t$ 个 token，$x_1, \ldots, x_{t-1}$ 是它**之前**的所有 token。直觉：给定前面所有的词，预测下一个词。这和第 8 章 Word2Vec 的 Skip-gram 有精神上的血缘关系，但这里的上下文是"所有前面的词"而不只是"相邻的几个词"。GPT 每一步都在做概率建模，因此天然适合文本生成。

**3. 微调（fine-tuning）的分类损失：**

$$\mathcal{L}_{\text{task}} = -\log P(y \mid \mathbf{h}_{[\text{CLS}]})$$

对于分类任务，BERT 在句子开头插入一个特殊标记 $[\text{CLS}]$，其对应的最终隐藏向量 $\mathbf{h}_{[\text{CLS}]}$ 被当作整个句子的"汇总表示"，再通过一个线性分类器输出类别概率。直觉：预训练阶段学到的丰富表示，只需要一个简单的分类头就能适应新任务——不需要重新设计网络结构。

### 手算例子

让我们手动展示 BERT 的遮盖语言模型是如何工作的。为了简洁，我们用一个非常迷你的设定。

**输入句子：** "The [MASK] sat on the [MASK]"

5 个位置（position 0 到 4），每个词有 3 维嵌入。经过多层 Transformer 后，我们直接给出 [MASK] 位置的输出向量。

**Step 1：构造输入。**

| 位置 | Token | 输入嵌入 $\mathbf{x}_i$（词嵌入 + 位置嵌入） |
|---|---|---|
| 0 | The | $(0.8, 0.1, 0.1)$ |
| 1 | [MASK] | $(0.0, 0.0, 0.0)$ |
| 2 | sat | $(0.1, 0.7, 0.2)$ |
| 3 | on | $(0.5, 0.3, 0.2)$ |
| 4 | [MASK] | $(0.0, 0.0, 0.0)$ |

注意 [MASK] 的初始嵌入是一个特殊向量（这里简化为零向量）——它不携带具体的词义信息，模型必须从上下文推断这个词是什么。

**Step 2：经过 Transformer 层（简化展示结果）。**

经过多头自注意力层，每个 [MASK] 位置的向量已经融合了左右两侧的上下文信息。假设 Transformer 输出为：

- 位置 1（第一个 [MASK]）的输出：$\mathbf{h}_1 = (0.6, 0.2, 0.8)$
- 位置 4（第二个 [MASK]）的输出：$\mathbf{h}_4 = (0.3, 0.7, 0.1)$

**Step 3：预测第一个 [MASK]（位置 1）。**

通过一个线性层将 $\mathbf{h}_1$ 投影到词汇表空间。假设词汇表只有 4 个候选词：

| 词 | 向量 $\mathbf{w}_k$ |
|---|---|
| cat | $(1.0, 0.3, 0.9)$ |
| dog | $(0.9, 0.2, 0.7)$ |
| car | $(0.1, 0.8, 0.1)$ |
| hat | $(0.5, 0.6, 0.3)$ |

计算每个词的得分 $z_k = \mathbf{h}_1 \cdot \mathbf{w}_k$（点积）：

- $z_{\text{cat}} = 0.6 \times 1.0 + 0.2 \times 0.3 + 0.8 \times 0.9 = 0.60 + 0.06 + 0.72 = 1.38$
- $z_{\text{dog}} = 0.6 \times 0.9 + 0.2 \times 0.2 + 0.8 \times 0.7 = 0.54 + 0.04 + 0.56 = 1.14$
- $z_{\text{car}} = 0.6 \times 0.1 + 0.2 \times 0.8 + 0.8 \times 0.1 = 0.06 + 0.16 + 0.08 = 0.30$
- $z_{\text{hat}} = 0.6 \times 0.5 + 0.2 \times 0.6 + 0.8 \times 0.3 = 0.30 + 0.12 + 0.24 = 0.66$

**Step 4：Softmax 得到概率。**

$$\exp(1.38) = 3.973, \quad \exp(1.14) = 3.126, \quad \exp(0.30) = 1.350, \quad \exp(0.66) = 1.934$$

总和 = $3.973 + 3.126 + 1.350 + 1.934 = 10.383$

$$P(\text{cat}) = \frac{3.973}{10.383} = 0.383, \quad P(\text{dog}) = \frac{3.126}{10.383} = 0.301$$

$$P(\text{car}) = \frac{1.350}{10.383} = 0.130, \quad P(\text{hat}) = \frac{1.934}{10.383} = 0.186$$

**"cat" 的概率最高（38.3%）！** 这合理吗？完全合理。第一个 [MASK] 的上下文是 "The \_\_\_ sat on the \_\_\_"——"坐"这个动作最常见的主语是动物（cat 或 dog），而 cat 的向量与上下文表示 $\mathbf{h}_1$ 的点积最高。

**Step 5：对第二个 [MASK] 做同样的事。**

$\mathbf{h}_4 = (0.3, 0.7, 0.1)$，假设候选词是 {mat, rug, bed, roof}。经过类似的点积 + softmax 计算（细节略），概率最高的应该是 "mat"——因为 "sat on the mat" 是最常见、最搭配的表达。

**关键观察：** 位置 1 的 [MASK] 在做预测时，利用了左侧的 "The" **和** 右侧的 "sat on the \_\_\_"——这就是 BERT "双向"（bidirectional）的威力。如果用 GPT 的单向方式，位置 1 只能看到 "The"，信息量少得多，预测 "cat" 的把握会低很多。反过来，GPT 的单向设计也有其独特的优势——它天然适合生成文本，因为生成过程本身就是"从左到右"的。

## 这解决了一切吗？——新的局限

BERT 和 GPT-1 在 2018 年的横空出世，彻底改变了 NLP 的游戏规则。BERT 在 11 项 NLP 基准测试上同时刷新了记录——这在以前是不可想象的。GPT-1 虽然在绝对分数上不及 BERT，但证明了生成式预训练同样是一条可行的道路。

但这个范式带来了三个新的问题。

**第一，模型应该做多大？** BERT 有两个版本：BERT-Base（1.1 亿参数）和 BERT-Large（3.4 亿参数）。BERT-Large 的效果更好，但训练成本也更高。如果继续增大模型，效果会线性提升吗？还是会遇到瓶颈？甚至边际收益递减？没有人知道答案。资源有限的研究者和公司面临着痛苦的抉择——该投入多少来训练更大的模型？

**第二，预训练数据的规模至关重要。** BERT 用了整个英文维基百科（约 25 亿词）加 BookCorpus（约 8 亿词），总共约 33 亿词。GPT-1 则仅用了 BookCorpus（约 8 亿词），规模远小于 BERT。但这够吗？如果用更多、更多样化的数据，效果会怎样？这些问题在当时都没有系统性的研究。

**第三，也是最根本的——"预训练 + 微调"范式的天花板在哪里？** 预训练的目标函数（完形填空或预测下一个词）只是一个代理任务（proxy task）——它不是我们真正关心的下游任务。模型在预训练中学到的能力，能否真正迁移到所有需要复杂推理、长文档理解的任务上？2018 年的初步结果很乐观，但更大的考验还在后面。

这些问题指向了一个更根本的追问：**模型规模、数据量、计算量与最终效果之间，是否存在某种可预测的数学关系？** 如果有，我们就能理性地知道"投入更多是否值得"——而不仅仅依靠直觉和运气。

## 这一年的世界

2018 年是 NLP 的"预训练元年"——也是 AI 从"学术竞赛"转向"工业军备竞赛"的关键转折点。

年初，AI2 的 ELMo 率先证明了预训练的语言表示可以有效提升多种 NLP 任务的表现，被评为 NAACL 2018 的最佳论文。紧接着，fast.ai 的 ULMFiT 用三阶段训练法（预训练 → 微调 → 任务适配）在文本分类上大幅超越了当时的最佳结果。这些工作为 BERT 和 GPT 铺平了道路——后人常说，2018 年初的 ELMo 和 ULMFiT 是预训练范式的"先遣队"，而 BERT 和 GPT 则是真正的主力军团。

6 月，OpenAI 发布了 GPT-1。10 月，Google 发布了 BERT。两篇论文一前一后，像是一场未经排练的双人舞——两个团队之间并没有事先沟通，却几乎同时抵达了同一个结论。BERT 的效果太过惊人，以至于 NLP 社区出现了一种微妙的情绪：既兴奋又焦虑。兴奋的是预训练范式如此有效；焦虑的是——训练 BERT 需要海量计算资源，普通大学实验室根本负担不起。有人戏称 NLP 已经变成了"富人俱乐部"的游戏。

在更大的图景中，2018 年也是 OpenAI 发生根本性转变的一年。2 月，Elon Musk 因与团队在发展方向上的分歧离开了董事会。失去了最大的资金支持者，OpenAI 面临存亡危机。但正是在这种压力下，团队做出了一个影响深远的决定：将研究重心从"多种 AI 方向并行探索"转向"全力构建大规模语言模型"。GPT-1 是这个方向的第一步——虽然它的影响在当时还不明显，但回过头来看，这是 OpenAI 从一个分散的研究实验室转变为高度聚焦的"大模型公司"的起点。

同年 5 月，欧盟的《通用数据保护条例》（GDPR）正式生效。这部法律虽然主要针对个人数据保护，但间接影响了 AI 训练数据的可用性——"用互联网上的公开文本训练模型"这一行为的合法性开始受到审视。这个法律阴影将在未来几年越来越大。

还有一个容易被忽视的细节：2018 年，一种叫 BPE（Byte Pair Encoding）的子词分词（subword tokenization）方法开始在 NLP 中广泛使用。BERT 使用的 WordPiece 和 GPT 使用的 BPE 都是将文本切分为子词单元的方法——它们让模型既能处理常见词（如 "the"），也能处理罕见词（如 "photosynthesis"= "photo" + "synthesis"），甚至能处理从未见过的词。这个看似不起眼的技术细节，实际上是大语言模型能处理任何文本的关键基础设施之一。

→ BERT 和 GPT 证明了"先预训练、再微调"的范式威力巨大，但一个关键问题悬而未决：模型应该做多大？更大的模型只是边际收益递减，还是会持续带来突破？如果投入更多计算，回报是否可预测？

