# 第 7 章 · 一条捷径改变一切——残差连接与 ResNet

> "When deeper networks are able to start converging, a degradation problem has been exposed: with the network depth increasing, accuracy gets saturated and then degrades rapidly. Unexpectedly, such degradation is not caused by overfitting." — Kaiming He et al., 2015

---

## 何恺明是谁？

2015 年的北京，中关村丹棱街 5 号——微软亚洲研究院（MSRA）的大楼里，一群年轻的研究员正在和一个令人沮丧的问题搏斗。

问题很简单：让神经网络更深。简单到听起来不像是一个"问题"——既然 AlexNet 证明了"越深越好"，那我们继续加深不就行了？

但现实没那么听话。把网络从 20 层加到 30 层，准确率反而下降了。加到 50 层，更差。这不是过拟合——连训练集上的误差都变大了。更深的网络理应至少不比浅网络差（大不了多出来的层什么都不学，直接把输入原封不动地传过去），但它就是更差。这个现象被称为**退化问题**（degradation problem），困扰了整个领域整整两年。

在 MSRA 的这间办公室里，一个安静的中国年轻人找到了答案。

**何恺明**（Kaiming He）1984 年出生在广州。和 AI 领域许多"从小就是天才"的故事不同，何恺明的学生时代更像一个"稳扎稳打的优等生"。2003 年，他以广东省理科第一名的成绩考入清华大学物理系基础科学班。在清华的四年里，他不是那种到处参加竞赛、拼命发论文的学生——他的同学们回忆说，何恺明最突出的特点是"思考问题时极其安静、极其专注"。

2007 年本科毕业后，何恺明去了香港中文大学（CUHK）攻读博士，师从 **汤晓鸥** 教授。他的博士论文研究的是图像中前景和背景的分离（image matting）——一个看似不起眼的问题，实际上要求精确到每个像素级别的判断。这段经历给了他一种对"细节"的敏感——他习惯于在最底层的数据流中寻找结构。

2011 年博士毕业后，何恺明加入微软亚洲研究院。在 MSRA，他遇到了两位重要的合作者。**孙剑**（Jian Sun）是 MSRA 的首席研究员，一个兼具学术洞察和工程直觉的罕见人才——他后来在旷视科技（Megvii）担任首席科学家，带领团队开发出了著名的人脸识别系统 Face++。**张祥雨**（Xiangyu Zhang）和 **任少卿**（Shaoqing Ren）则是 MSRA 的年轻研究员，同样痴迷于让网络更深。

2015 年初，他们开始系统性地研究退化问题。实验结果令人困惑：无论怎么调参、怎么初始化、怎么正则化，30 层网络就是比 20 层差。何恺明后来在一次演讲中描述了他们当时的感受：**"这不应该是过拟合，因为训练误差也在变高。但我们不知道为什么。"**

然后，一个极其简单的想法出现了。

他们没有试图让每一层学习完整的映射，而是让每一层只学习**残差**——"输入和输出之间的差值"。如果最优映射恰好是"什么都不做"（恒等映射），那网络只需要把残差推到零就行了——比让一堆非线性层去学习"恒等"容易得多。

这个想法需要一个物理上的实现：一条绕过每层的"捷径"（shortcut connection），把输入直接加到输出上。他们把这个结构叫做**残差块**（residual block），把整个网络叫做 **ResNet**。

2015 年 12 月，论文在 arXiv 上公开，随后被 CVPR 2016 接收。结果震惊了所有人：一个 152 层的网络，在 ImageNet 上取得了 3.57% 的 top-5 错误率——**第一次，机器在图像识别上超越了人类**（人类的错误率大约是 5.1%）。而这个网络只有 AlexNet 层数的 19 倍，参数量反而更少。

ResNet 论文成了计算机视觉领域有史以来被引用最多的论文之一（超过 20 万次）。但何恺明本人始终保持着一种低调的风格——他不热衷于在社交媒体上发表观点，不频繁出现在科技媒体的采访中，而是持续地、安静地做出一个又一个重要贡献：ResNeXt、Mask R-CNN、Group Normalization……在 FAIR（Facebook AI Research，后改名为 Meta AI），他成为深度学习领域最有影响力的研究者之一。


## 上一章的遗产：一个没解决的问题

第 6 章的 AlexNet 用 8 层网络横扫了 ImageNet。从那以后，整个领域的信条变成了一句简单的话：**越深越好**。

2014 年，两篇论文把这条信条推到了新的极端。牛津大学的 **VGGNet** 用 16-19 层的小卷积核堆叠，证明了"统一架构 + 更深"的策略有效。Google 的 **GoogLeNet**（也叫 Inception-v1）用 22 层的精心设计结构，进一步提升了准确率。

于是所有人都在想同一个问题：能不能继续加？30 层？50 层？100 层？

但实验结果像是一盆冷水。

何恺明的团队做了一个系统的实验：在 CIFAR-10 数据集上，分别训练一个 20 层网络和一个 56 层网络。除了深度不同，两个网络的其他设置完全一样——相同的优化器、相同的学习率、相同的正则化。

结果是：**56 层网络在训练集和测试集上的误差都比 20 层网络高。**

这完全说不通。想想看：一个 56 层网络完全可以做到和 20 层网络一样好——只要前 20 层学到和 20 层网络完全相同的映射，后面 36 层学到恒等映射（输入什么就输出什么）就行了。理论上，更深的网络至少不会更差。但实际上它就是更差了。

这不是过拟合——过拟合的特征是训练集上表现好、测试集上表现差。而这里的 56 层网络连训练集上的表现都更差。这说明问题出在**优化**上：梯度下降找不到那个好的解。

这个现象被命名为**退化问题**（degradation problem）。它不是梯度消失——Batch Normalization（批量归一化，2015 年由 Ioffe 和 Szegedy 提出）已经很大程度上解决了梯度消失，前向传播的信号在 50 层网络中仍然保持健康。问题更微妙：梯度下降在深层网络的参数空间中，很难找到通往好解的路径。

换句话说：**路是通的，但太弯了，优化器迷路了。**

## 核心洞察：一句话说清楚

**与其让每一层学习从输入到输出的完整映射，不如让它只学习"差值"——输入和目标输出之间的残差。再给数据一条绕过所有层的捷径，让它始终可以"什么都不做"地流过去。**

想象你在教一个学生画画。传统方法是让他在一张白纸上从零开始画出蒙娜丽莎——太难了。残差学习的方法是：给他一张蒙娜丽莎的照片，让他在上面做修改——加深阴影、调整嘴角的弧度。如果不需要修改，他就什么都不做。修改（残差）比从零创造（完整映射）容易得多。

或者用建筑来比喻。传统深层网络像一座没有楼梯的摩天大楼——你必须从一楼爬外墙到五十楼，中间只要一步踏空就摔下来。残差网络给每层之间修了一部电梯（捷径连接）——即使某一层的"改造"失败了，数据也可以坐着电梯直接上楼，不会损失任何信息。

## 数学原理

### 直觉解释

让我们用两种方式理解残差连接为什么有效。

**从"学什么"的角度。** 假设某一层的最优映射是 $H(x) = x$（恒等映射）。在传统网络中，一层非线性变换要学习 $\sigma(Wx + b) = x$——让一堆 ReLU、矩阵乘法和偏置组合起来，恰好等于输入本身。这不难吗？用 ReLU 的话，$\max(0, Wx + b) = x$ 要求 $Wx + b$ 在所有位置都恰好等于 $x$（因为 $x$ 可正可负）。这个约束非常严格，优化器很难找到满足它的参数。

残差学习把这个目标改了：不再要求 $H(x) = x$，而是要求 $F(x) = H(x) - x = 0$。让一堆参数恰好等于零，比让一堆参数恰好抵消出一个恒等映射，容易得多——把所有权重初始化为接近零就行了。

**从"梯度怎么流"的角度。** 这可能是残差连接更深刻的价值。在传统深层网络中，梯度通过链式法则从后向前传播，每经过一层都要乘以一个因子。经过几十层之后，这些因子的乘积可能变得极小（梯度消失）或极不稳定（梯度爆炸）。即使有 ReLU 和 Batch Normalization，梯度仍然需要"穿过"每一层——而每一层的参数都是被优化器调整过的，梯度的路径完全取决于这些参数的当前值。

残差连接给了梯度一条**不经过任何非线性变换的直达通道**。考虑最简单的残差块：$y = F(x) + x$。反向传播时，梯度从 $y$ 传到 $x$：

$$\frac{\partial y}{\partial x} = \frac{\partial F(x)}{\partial x} + 1$$

注意那个 **"+1"**——不管 $F(x)$ 的梯度是什么，总有一个"1"保证梯度至少可以原封不动地传回去。这就像在一条崎岖的山路上修了一条隧道：不管山路多难走，隧道始终畅通。

当多个残差块堆叠在一起时，这个 "+1" 效应会累积。梯度从第 $L$ 层传到第 $l$ 层：

$$\frac{\partial \mathcal{L}}{\partial x_l} = \frac{\partial \mathcal{L}}{\partial x_L} \cdot \prod_{i=l}^{L-1}\left(1 + \frac{\partial F(x_i)}{\partial x_i}\right)$$

展开乘积后，每一项都包含一个 "+1" 的基底。即使某些 $\frac{\partial F}{\partial x}$ 很小甚至为零，整个乘积也不会消失——因为每一步都有一条"直通车道"。这就是为什么 ResNet 可以训练 152 层甚至 1000 层的网络。

### 关键公式

**1. 残差块——基本构建单元：**

$$\mathbf{y} = \mathcal{F}(\mathbf{x}, \{W_i\}) + \mathbf{x}$$

其中 $\mathbf{x}$ 是输入，$\mathbf{y}$ 是输出，$\mathcal{F}(\mathbf{x}, \{W_i\})$ 是残差映射（通常由 2-3 个带权重和激活函数的层组成），$\{W_i\}$ 是这些层的参数。$+\mathbf{x}$ 就是捷径连接（shortcut connection）——它不引入任何额外参数，也不增加计算量，只是在输出上加上了原始输入。

**2. 梯度的直通保障：**

$$\frac{\partial \mathcal{L}}{\partial \mathbf{x}} = \frac{\partial \mathcal{L}}{\partial \mathbf{y}} \cdot \left(\frac{\partial \mathcal{F}}{\partial \mathbf{x}} + \mathbf{I}\right)$$

其中 $\mathbf{I}$ 是单位矩阵。关键洞察：即使 $\frac{\partial \mathcal{F}}{\partial \mathbf{x}} = \mathbf{0}$（残差分支完全不传导梯度），梯度仍然可以通过 $\mathbf{I}$ 这条通道无损传递。传统网络没有这个"保底"——它的梯度传播完全依赖于每一层的参数，一旦某一层"堵死"，后面的层就全部断联。

**3. 跨多个残差块的梯度流：**

假设有 $L - l$ 个残差块堆叠，从第 $l$ 层到第 $L$ 层。可以写出：

$$\mathbf{x}_L = \mathbf{x}_l + \sum_{i=l}^{L-1} \mathcal{F}(\mathbf{x}_i, \{W_i\})$$

这表示：**最终输出等于初始输入加上所有残差的总和**。梯度反向传播时：

$$\frac{\partial \mathcal{L}}{\partial \mathbf{x}_l} = \frac{\partial \mathcal{L}}{\partial \mathbf{x}_L}\left(1 + \frac{\partial}{\partial \mathbf{x}_l}\sum_{i=l}^{L-1}\mathcal{F}(\mathbf{x}_i, \{W_i\})\right)$$

括号里的 "1" 保证信息不丢失，求和项提供"附加路径"让梯度可以从任意中间层渗入。这种"多路径"结构是 ResNet 训练稳定性的根本来源。

### 手算例子

让我们用一个极简的例子对比传统网络和残差网络的梯度传播。

**设定。** 一个 3 层网络，每层是一个简单的线性变换 $h_{i} = W_i \cdot h_{i-1}$（为简化计算，暂不使用激活函数）。输入 $x = 1.0$，目标输出 $t = 2.0$。

传统网络的权重：
$$W_1 = 0.5, \quad W_2 = 0.5, \quad W_3 = 0.5$$

**传统网络：前向传播。**
$$h_1 = W_1 \cdot x = 0.5 \times 1.0 = 0.5$$
$$h_2 = W_2 \cdot h_1 = 0.5 \times 0.5 = 0.25$$
$$h_3 = W_3 \cdot h_2 = 0.5 \times 0.25 = 0.125$$

损失：$\mathcal{L} = \frac{1}{2}(h_3 - t)^2 = \frac{1}{2}(0.125 - 2.0)^2 = \frac{1}{2}(3.516) = 1.758$

**传统网络：反向传播。** 损失对输出的梯度：
$$\frac{\partial \mathcal{L}}{\partial h_3} = h_3 - t = 0.125 - 2.0 = -1.875$$

梯度传到 $h_2$：
$$\frac{\partial \mathcal{L}}{\partial h_2} = \frac{\partial \mathcal{L}}{\partial h_3} \times W_3 = -1.875 \times 0.5 = -0.9375$$

梯度传到 $h_1$：
$$\frac{\partial \mathcal{L}}{\partial h_1} = \frac{\partial \mathcal{L}}{\partial h_2} \times W_2 = -0.9375 \times 0.5 = -0.46875$$

梯度传到输入：
$$\frac{\partial \mathcal{L}}{\partial x} = \frac{\partial \mathcal{L}}{\partial h_1} \times W_1 = -0.46875 \times 0.5 = -0.234$$

注意：经过 3 层，梯度从 $-1.875$ 缩小到 $-0.234$——缩小了 $0.5^3 = 8$ 倍。如果是 50 层网络，梯度将缩小 $0.5^{50}$ 倍——基本为零。

**残差网络：前向传播。** 现在每一层输出 $y_i = W_i \cdot h_{i-1} + h_{i-1}$（加入残差连接），即 $y_i = (W_i + 1) \cdot h_{i-1}$。

等价权重：
$$\tilde{W}_1 = W_1 + 1 = 1.5, \quad \tilde{W}_2 = 1.5, \quad \tilde{W}_3 = 1.5$$

前向传播：
$$h_1 = 1.5 \times 1.0 = 1.5$$
$$h_2 = 1.5 \times 1.5 = 2.25$$
$$h_3 = 1.5 \times 2.25 = 3.375$$

损失：$\mathcal{L} = \frac{1}{2}(3.375 - 2.0)^2 = \frac{1}{2}(1.891) = 0.945$

仅靠残差连接的 "+1"，输出已经从 0.125 提升到了 3.375，损失从 1.758 降低到了 0.945。

**残差网络：反向传播。**

$$\frac{\partial \mathcal{L}}{\partial h_3} = 3.375 - 2.0 = 1.375$$

梯度传到 $h_2$：
$$\frac{\partial \mathcal{L}}{\partial h_2} = \frac{\partial \mathcal{L}}{\partial h_3} \times \tilde{W}_3 = 1.375 \times 1.5 = 2.0625$$

梯度传到 $h_1$：
$$\frac{\partial \mathcal{L}}{\partial h_1} = 2.0625 \times 1.5 = 3.09375$$

梯度传到输入：
$$\frac{\partial \mathcal{L}}{\partial x} = 3.09375 \times 1.5 = 4.641$$

**关键对比：**

| | 传统网络 | 残差网络 |
|---|---|---|
| 输出值 | 0.125 | 3.375 |
| 损失 | 1.758 | 0.945 |
| 输入层梯度 | -0.234 | 4.641 |

传统网络的梯度在传播过程中**缩小**了 8 倍，而残差网络的梯度反而**放大**了。这就是 "+1" 的力量：每经过一层，梯度不仅不衰减，还叠加了一条直通通道。

当然，这个线性例子是简化过的——真实网络中还有 ReLU 和 Batch Normalization 等非线性操作。但核心原理完全一样：残差连接在每一步都保证了一个 $1 + \frac{\partial F}{\partial x}$ 的梯度因子，其中 "1" 是保底，$\frac{\partial F}{\partial x}$ 是增益。这个 "1" 就是 ResNet 能够训练上百层网络的秘密。

## 这解决了一切吗？——新的局限

ResNet 的成功是深远的。它不仅在 ImageNet 上超越了人类，更重要的是，**残差连接成了一种通用架构组件**。后来的几乎所有神经网络——无论处理图像、文本还是音频——都采用了某种形式的跳跃连接。第 10 章的 Transformer 每一层都包含残差连接；GPT、BERT 等预训练语言模型也依赖它来训练几十层甚至上百层的网络。可以说，没有残差连接，就没有后来的一切。

但 ResNet 也留下了三个重要的局限。

**第一，更深不等于更聪明。** ResNet 让网络从 8 层深到 152 层，图像识别准确率超过了人类。但"识别图片中的猫"和"理解一段文字的含义"是完全不同的问题。ResNet 解决的是视觉领域的深度问题，而自然语言处理（NLP）仍然在等待自己的突破口。一个只能看图不能读书的 AI，就像一个视觉敏锐但目不识丁的人。

**第二，"黑箱"问题更加严重了。** 8 层的 AlexNet 已经让研究者头疼——你很难解释为什么某个神经元对某种图像模式有响应。现在网络到了 152 层，有数千万个参数，理解它的内部运作几乎是不可行的。我们知道它管用，但不知道它为什么管用。这种"不可解释性"在学术圈和产业界都引发了持续的争论——尤其在 AI 开始应用于医疗诊断、自动驾驶等高风险场景时。

**第三，效率问题依然存在。** 尽管 ResNet 的捷径连接没有增加参数量，但 152 层网络的计算量仍然巨大。2015 年的 ResNet 需要数十亿次浮点运算来处理一张图片。如果要把类似的深度架构应用到自然语言——处理数万个词的序列——计算量会更加惊人。如何让深而强大的模型变得足够高效，是一个悬而未决的问题。

这些局限指向了一个共同的结论：**深度学习的下一步突破，不会仅仅来自"更深"。它需要来自全新的方向。** 而在当时，有两个方向正在同时酝酿——一个在语言的领域，试图让机器理解词语之间的距离；另一个在架构的层面，试图让机器学会"看哪里重要"。

→ ResNet 让视觉网络从 8 层飞跃到 152 层，图像识别准确率首次超越人类——但自然语言仍然被困在 one-hot 向量的荒漠里。词与词之间没有距离、没有相似度、没有语义。谁能把词语放进一个有意义的数学空间？

## 这一年的世界

2015 年，深度学习从一个学术方向正式变成了一场产业运动。

11 月，Google 公开了 **TensorFlow**——一个开源的深度学习框架。在此之前，如果你想做深度学习研究，基本上要么自己写 C++/CUDA 代码，要么用 Theano（一个主要由蒙特利尔大学维护的学术工具）。TensorFlow 的出现让深度学习的门槛大幅降低——一个研究生花一个下午就能搭出一个卷积网络。这直接催生了 2015-2018 年间深度学习应用的爆发式增长。

10 月，一个更震撼的消息传来：Google DeepMind 的 **AlphaGo** 击败了欧洲围棋冠军樊麾（Fan Hui）——这是 AI 第一次在公平比赛中击败职业围棋选手。围棋长期被认为是"机器不可能征服"的游戏。AlphaGo 用的不仅是深度学习，还有强化学习和蒙特卡洛树搜索的结合——这种"混合"策略后来成为 AI 领域的重要范式。

12 月，**OpenAI** 在旧金山成立。创始团队包括 Sam Altman、Elon Musk、Ilya Sutskever（我们在第 6 章见过他——AlexNet 的共同作者）、Greg Brockman 等人。OpenAI 的创始宣言承诺要确保通用人工智能（AGI）惠及全人类。这个宣言在当时看起来有些理想主义甚至天真——一家只有几个人的非营利组织，怎么能确保如此宏大的目标？但十年后，正是这家组织发布了 GPT-4，成为了全球最有影响力的 AI 公司。

在中国，AI 的产业应用也在加速。旷视科技（Megvii）的 Face++ 人脸识别系统开始在支付宝和滴滴中应用；科大讯飞的语音识别技术嵌入了中国大多数智能手机。中国政府开始将 AI 列为国家战略——这份战略在两年后以国务院文件的形式正式发布，目标是到 2030 年让中国成为"全球领先的人工智能创新中心"。

与此同时，学术界发生了一件意味深长的事：**Yann LeCun、Yoshua Bengio 和 Geoffrey Hinton**——后来被称为"深度学习三巨头"的三个人——在 *Nature* 杂志上发表了一篇综述论文，标题就叫 *"Deep Learning"*。这篇论文不是报告新发现，而是向整个科学界宣告：深度学习不再是一个边缘方向，它是主流。十年前被嘲笑的神经网络，如今登上了科学界最负盛名的期刊。

但讽刺的是，正是同一年的 ResNet 论文暗示了一个更深层的问题：我们已经知道如何让网络变得任意深、任意强大——但我们仍然不知道它在做什么。从第 1 章的感知机到 152 层的 ResNet，58 年过去了。我们造出了一个超越人类视觉的系统，但理解它为什么工作，可能比建造它更难。
